---
phase: 07
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - driver/resources/settings/default.vrsettings
autonomous: true
requirements: [MIG-06]
must_haves:
  truths:
    - "default.vrsettings parses as valid JSON with 9 keys under `driver_micmap` (4 existing + 5 new)"
    - "`enable_driver_detection` key exists with boolean value `false` (D-13, D-27)"
    - "`detection_sensitivity` key exists with float value `0.7` (D-13)"
    - "`detection_threshold` key exists with float value `0.6` (D-13)"
    - "`detection_cooldown_ms` key exists with int32 value `1000` (D-13)"
    - "`detection_min_duration_ms` key exists with int32 value `200` (D-13)"
    - "Existing P6 keys (`enable`, `http_port`, `http_host`, `enable_driver_audio`) remain byte-identical (no drift)"
    - "`enable_driver_detection` default value is hardcoded `false` so SC4 byte-identical-to-P6-closeout regression holds (D-27)"
  artifacts:
    - path: "driver/resources/settings/default.vrsettings"
      provides: "5 new VRSettings keys for driver-side detection (read once at DeviceProvider::Init via VRSettings GetBool/GetFloat/GetInt32 in 07-05)"
      contains: "enable_driver_detection"
  key_links:
    - from: "driver/resources/settings/default.vrsettings"
      to: "driver/src/device_provider.cpp (Init step 5, lands in 07-05)"
      via: "vr::VRSettings()->GetBool/GetFloat/GetInt32(\"driver_micmap\", \"detection_*\", &err) — single-read pattern"
      pattern: "enable_driver_detection|detection_sensitivity|detection_threshold|detection_cooldown_ms|detection_min_duration_ms"
---

<objective>
Wave 1 — append 5 new VRSettings keys to `driver/resources/settings/default.vrsettings` per D-13. Tiny but load-bearing: these are the keys 07-05's DeviceProvider::Init reads once at startup, the keys 07-09's UAT D-25(1) sets to true on the Bigscreen Beyond rig, and the keys whose defaults mirror v1.5 client-side `DetectionConfig` defaults so P8's `PUT /settings` cutover doesn't silently drift the runtime semantics.

Purpose: ship the schema half of D-13 in a standalone plan so it can land in Wave 1 in parallel with 07-03's DetectionRunner authoring. No code touches this file; no code depends on this file at compile-time (it's runtime config); no other plan touches this file (zero file conflict). Trivially parallelizable.

Output: One JSON file edit. Five new keys appended to the existing `driver_micmap` block. Existing 4 keys preserved byte-for-byte. Default values mirror v1.5 client-side `DetectionConfig` (`src/core/include/micmap/core/config_manager.hpp:28-33`) where the schema overlaps; PATTERNS.md notes the `min_duration_ms` (200 vs 300) and `cooldown_ms` (1000 vs 300) differences are LOCKED per CONTEXT D-13.
</objective>

<execution_context>
@C:/Users/decid/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@C:/Users/decid/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-driver-side-detection-thread/07-CONTEXT.md
@.planning/phases/07-driver-side-detection-thread/07-PATTERNS.md
@./CLAUDE.md
@driver/resources/settings/default.vrsettings
@src/core/include/micmap/core/config_manager.hpp

<interfaces>
<!-- Final file shape per 07-PATTERNS.md §"`driver/resources/settings/default.vrsettings` MODIFIED" -->

```json
{
    "driver_micmap": {
        "enable": true,
        "http_port": 27015,
        "http_host": "127.0.0.1",
        "enable_driver_audio": false,
        "enable_driver_detection": false,
        "detection_sensitivity": 0.7,
        "detection_threshold": 0.6,
        "detection_cooldown_ms": 1000,
        "detection_min_duration_ms": 200
    }
}
```

# v1.5 client-side DetectionConfig defaults (src/core/include/micmap/core/config_manager.hpp:28-33):
# - sensitivity = 0.7f       → matched (D-13 sensitivity 0.7)
# - minDurationMs = 300       → DRIFTED to 200 in driver (D-13 LOCKS this — document in commit message)
# - cooldownMs = 300          → DRIFTED to 1000 in driver (D-13 LOCKS this — document in commit message)
# - fftSize = 2048            → not exposed via VRSettings (driver-side hardcoded in 07-03 createFFTDetector(_, 2048))
#
# threshold = 0.6 has NO v1.5 client-side counterpart in DetectionConfig — P7 introduces it as a new tunable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 5 detection keys to default.vrsettings (D-13)</name>
  <files>driver/resources/settings/default.vrsettings</files>
  <read_first>
    - driver/resources/settings/default.vrsettings (full file — 8 lines, exact JSON shape to extend; existing keys MUST stay byte-identical)
    - .planning/phases/07-driver-side-detection-thread/07-PATTERNS.md §"`driver/resources/settings/default.vrsettings` MODIFIED — append 5 keys (D-13)" (final-shape excerpt; v1.5 default-mirror table)
    - .planning/phases/07-driver-side-detection-thread/07-CONTEXT.md (D-13 schema, D-27 default-OFF flag discipline, D-28 deployment caveat)
    - .planning/phases/07-driver-side-detection-thread/07-RESEARCH.md §"`default.vrsettings` extension (D-13)" (final shape) + §"Specifics" warning about value-mirror documentation
    - src/core/include/micmap/core/config_manager.hpp lines 28-33 (v1.5 client-side DetectionConfig — for the divergence-on-min_duration/cooldown documentation in the commit message)
    - .planning/phases/06-driver-side-audio-capture-spike/06-02-PLAN.md (P6 D-01 analog — same single-key-append pattern; final JSON shape includes `enable_driver_audio`)
  </read_first>
  <behavior>
    - File parses as valid JSON.
    - `driver_micmap.enable_driver_detection` is `false` (boolean, not string "false").
    - `driver_micmap.detection_sensitivity` is `0.7` (number — JSON parsers will read as float).
    - `driver_micmap.detection_threshold` is `0.6` (number).
    - `driver_micmap.detection_cooldown_ms` is `1000` (integer, not float — vrsettings GetInt32 contract).
    - `driver_micmap.detection_min_duration_ms` is `200` (integer).
    - Existing keys (`enable`, `http_port`, `http_host`, `enable_driver_audio`) remain byte-identical to the pre-change file.
    - Indentation: 4 spaces, mirrors existing file.
    - JSON commas: every key except the last has a trailing comma; last key (`detection_min_duration_ms`) has NO trailing comma.
    - File ends with a single trailing newline.
  </behavior>
  <action>
**Edit `driver/resources/settings/default.vrsettings`.** The pre-change file (verified at P6 closeout) is:

```json
{
    "driver_micmap": {
        "enable": true,
        "http_port": 27015,
        "http_host": "127.0.0.1",
        "enable_driver_audio": false
    }
}
```

After this task, the file MUST be EXACTLY:

```json
{
    "driver_micmap": {
        "enable": true,
        "http_port": 27015,
        "http_host": "127.0.0.1",
        "enable_driver_audio": false,
        "enable_driver_detection": false,
        "detection_sensitivity": 0.7,
        "detection_threshold": 0.6,
        "detection_cooldown_ms": 1000,
        "detection_min_duration_ms": 200
    }
}
```

Concrete edit recipe:
1. Open `driver/resources/settings/default.vrsettings`.
2. After the line `        "enable_driver_audio": false`, change the trailing newline to `,\n` (add a comma).
3. Insert 5 new lines between that line and the closing `    }`:
   ```
           "enable_driver_detection": false,
           "detection_sensitivity": 0.7,
           "detection_threshold": 0.6,
           "detection_cooldown_ms": 1000,
           "detection_min_duration_ms": 200
   ```
   The first 4 new lines have a trailing comma; the last (`detection_min_duration_ms`) has NO trailing comma.
4. Indentation: 8 spaces (matches the existing keys' indent — 4 for the `driver_micmap` block + 4 for its inner keys).
5. Preserve the trailing newline at EOF.

CRITICAL constraints (each is a hard acceptance criterion):
1. The 4 existing keys MUST stay BYTE-IDENTICAL — same indentation, same values, same key order. Only the trailing-comma on `enable_driver_audio: false` changes (was no comma; becomes comma).
2. JSON validity — `python -c "import json; json.load(open('driver/resources/settings/default.vrsettings'))"` exits 0.
3. `enable_driver_detection` value MUST be the JSON boolean `false`, not the string `"false"` (D-27 — flag-default-OFF; SC4 byte-identical-to-P6 regression hinges on this).
4. `detection_cooldown_ms` and `detection_min_duration_ms` MUST be JSON integers (no decimal point) — vrsettings `GetInt32` contract requires this; using `1000.0` or `200.0` would break the read in 07-05.

In the commit message (NOT inside the JSON file — JSON has no comments), document the v1.5 default mirror per the PATTERNS.md "Specifics" warning:
> Driver-side defaults differ from v1.5 client-side `DetectionConfig` on `min_duration_ms` (200 vs 300) and `cooldown_ms` (1000 vs 300). LOCKED per CONTEXT.md D-13. P8 cutover (when driver becomes config.json reader) will surface this delta — document it then.

The CMake `POST_BUILD` copy in `driver/CMakeLists.txt:148` propagates the file to `build/driver/micmap/resources/settings/default.vrsettings` automatically; no CMake changes needed in this task.
  </action>
  <verify>
    <automated>python -c "import json,sys; d=json.load(open(r'C:/Users/decid/Documents/projects/mic-map/driver/resources/settings/default.vrsettings')); s=d['driver_micmap']; assert s['enable']==True and s['http_port']==27015 and s['http_host']=='127.0.0.1' and s['enable_driver_audio']==False and s['enable_driver_detection']==False and abs(s['detection_sensitivity']-0.7)<1e-9 and abs(s['detection_threshold']-0.6)<1e-9 and s['detection_cooldown_ms']==1000 and s['detection_min_duration_ms']==200 and isinstance(s['detection_cooldown_ms'],int) and isinstance(s['detection_min_duration_ms'],int); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `driver/resources/settings/default.vrsettings` is valid JSON (`python -c "import json; json.load(open(...))"` exits 0).
    - The 5 new keys are present at exact paths `driver_micmap.enable_driver_detection`, `driver_micmap.detection_sensitivity`, `driver_micmap.detection_threshold`, `driver_micmap.detection_cooldown_ms`, `driver_micmap.detection_min_duration_ms`.
    - `grep -c "enable_driver_detection" driver/resources/settings/default.vrsettings` == 1.
    - `grep -c "detection_sensitivity" driver/resources/settings/default.vrsettings` == 1.
    - `grep -c "detection_threshold" driver/resources/settings/default.vrsettings` == 1.
    - `grep -c "detection_cooldown_ms" driver/resources/settings/default.vrsettings` == 1.
    - `grep -c "detection_min_duration_ms" driver/resources/settings/default.vrsettings` == 1.
    - `python -c "import json; d=json.load(open(r'driver/resources/settings/default.vrsettings')); s=d['driver_micmap']; assert s['enable_driver_detection'] is False"` exits 0 (D-27 — boolean false, default OFF).
    - `python -c "import json; d=json.load(open(r'driver/resources/settings/default.vrsettings')); s=d['driver_micmap']; assert isinstance(s['detection_cooldown_ms'], int) and not isinstance(s['detection_cooldown_ms'], bool)"` exits 0 (int, not float, not bool).
    - `python -c "import json; d=json.load(open(r'driver/resources/settings/default.vrsettings')); s=d['driver_micmap']; assert isinstance(s['detection_min_duration_ms'], int)"` exits 0.
    - `python -c "import json; d=json.load(open(r'driver/resources/settings/default.vrsettings')); s=d['driver_micmap']; assert abs(s['detection_sensitivity'] - 0.7) < 1e-9 and abs(s['detection_threshold'] - 0.6) < 1e-9"` exits 0 (float defaults match D-13).
    - `python -c "import json; d=json.load(open(r'driver/resources/settings/default.vrsettings')); s=d['driver_micmap']; assert s['enable']==True and s['http_port']==27015 and s['http_host']=='127.0.0.1' and s['enable_driver_audio']==False"` exits 0 (P6 keys preserved byte-identical).
    - `python -c "import json; d=json.load(open(r'driver/resources/settings/default.vrsettings')); s=d['driver_micmap']; assert len(s)==9"` exits 0 (4 P6 keys + 5 new keys; no extra keys, no missing keys).
    - cmake configure step succeeds (`cmake -B build-headless -S . -DMICMAP_BUILD_DRIVER=OFF` exits 0).
    - Existing P5 + P6 + 07-01 ctest invariants stay green:
      - `ctest --test-dir build-headless -R AssertNoOpenVRInCore --output-on-failure` exits 0.
      - `ctest --test-dir build-headless -R lint_no_openvr_in_core --output-on-failure` exits 0.
      - `ctest --test-dir build-headless -R AssertAudioWorkerNoVrApi --output-on-failure` exits 0.
      - `ctest --test-dir build-headless -R AssertDetectionRunnerNoVrApi --output-on-failure` exits 0.
  </acceptance_criteria>
  <done>The 5 new detection keys are present in default.vrsettings with correct types + defaults (enable_driver_detection=false enforces D-27 SC4 regression). Existing P6 keys are byte-identical. JSON validates. The schema is ready for 07-05's `DeviceProvider::Init` single-read pattern (D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installer / SteamVR resource loader → driver_micmap.dll | `default.vrsettings` is read once at driver Init via `vr::VRSettings()`; values become detection-thread state. Schema drift between this file and the 07-05 read code = silent default-substitution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T (Tampering — JSON corruption silently disables flag) | default.vrsettings parser | mitigate | Acceptance criteria assert each key's exact value via python json.load + isinstance checks. The vrsettings VR_SettingsError_UnsetSettingHasNoDefault path in 07-05 falls through to explicit defaults that mirror this file — so even if a future edit corrupts the JSON, the fail-soft defaults match. |
| T-07-02-02 | I (Information disclosure — surprise enablement on shipped users) | enable_driver_detection default | mitigate | `enable_driver_detection` default value is hardcoded `false` (D-27); 07-05's Init logic constructs no DetectionRunner when flag is OFF; SC4 byte-identical-to-P6 invariant holds. Acceptance: python assert `s['enable_driver_detection'] is False`. |
| T-07-02-03 | T (Tampering — int-vs-float type confusion in vrsettings GetInt32) | detection_cooldown_ms / detection_min_duration_ms | mitigate | Acceptance criteria assert `isinstance(s[…], int)`. Writing `1000.0` would parse as float and `vr::VRSettings()->GetInt32` would either silently truncate or return UnsetSettingHasNoDefault — both bad. The acceptance test catches this at PR-time. |
| T-07-02-04 | I (Information disclosure — silent default drift between v1.5 client and v1.6 driver) | detection_min_duration_ms (200 vs 300), detection_cooldown_ms (1000 vs 300) | accept | Per CONTEXT.md D-13 LOCKED decision. Documented in commit message per RESEARCH.md "Specifics" warning. P8 cutover (when driver becomes config.json reader) will resolve the delta one way or another; for P7 the divergence is a known feature, not a defect. |
</threat_model>

<verification>
**After the task lands:**

```bash
# JSON validates and contents are correct
python -c "import json; d=json.load(open(r'driver/resources/settings/default.vrsettings')); print(d['driver_micmap'])"

# Configure passes
cmake -B build-headless -S . -DMICMAP_BUILD_DRIVER=OFF

# All carryover invariants stay green
ctest --test-dir build-headless --output-on-failure -R "AssertNoOpenVRInCore|lint_no_openvr_in_core|AssertAudioWorkerNoVrApi|AssertDetectionRunnerNoVrApi"
```
</verification>

<success_criteria>
- `default.vrsettings` parses as JSON with 9 keys under `driver_micmap` (4 P6 + 5 new).
- All 5 new keys have the exact types and values from D-13 (`enable_driver_detection=false`, `detection_sensitivity=0.7f`, `detection_threshold=0.6f`, `detection_cooldown_ms=1000` int, `detection_min_duration_ms=200` int).
- Existing 4 P6 keys remain byte-identical (zero drift).
- All P5 / P6 / 07-01 ctest invariants remain green.
- The schema is ready for 07-05's single-read VRSettings block.
</success_criteria>

<output>
After completion, create `.planning/phases/07-driver-side-detection-thread/07-02-SUMMARY.md`
</output>
