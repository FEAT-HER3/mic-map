---
phase: 08-ipc-contract-reshape
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - cmake/AssertNoJsonInCore.cmake
  - cmake/AssertHttpServerLocalhostOnly.cmake
  - cmake/AssertHttpServerNoVrApi.cmake
  - cmake/AssertNoConfigWriteInClient.cmake
  - tests/driver/get_state_shape_test.cpp
  - tests/driver/get_telemetry_level_test.cpp
  - tests/driver/get_devices_cache_test.cpp
  - tests/driver/get_settings_shape_test.cpp
  - tests/driver/put_settings_round_trip_test.cpp
  - tests/driver/put_settings_validation_test.cpp
  - tests/driver/put_settings_stress100_test.cpp
  - tests/driver/init_config_share_violation_test.cpp
  - tests/driver/state_clear_error_test.cpp
  - tests/driver/config_io_atomic_persist_test.cpp
  - tests/test_multi_sink_logger.cpp
  - tests/test_settings_validator.cpp
  - tests/test_client_driver_loaded_indicator.cpp
  - tests/test_client_level_meter_cadence.cpp
  - tests/CMakeLists.txt
autonomous: true
requirements: [IPC-04, IPC-05, IPC-07, IPC-08, LIB-04, HEALTH-01, HEALTH-06]
tags: [phase-8, ipc, wave-0, lints, red-tolerant]

must_haves:
  truths:
    - "All 4 new lint scripts exist under cmake/ and run cleanly via ctest at Wave 0 (skip-on-NOT-EXISTS branches keep them GREEN)"
    - "All 16 driver/client RED-tolerant test scaffolds compile to skeletons; tests/CMakeLists.txt registers them under EXISTS-gated source-list pattern from P7"
    - "ctest --test-dir build runs all new lints + scaffold registrations (no FATAL during configure)"
  artifacts:
    - path: "cmake/AssertNoJsonInCore.cmake"
      provides: "lint asserting <nlohmann/json.hpp> absent under src/audio, src/detection, src/core, src/common"
      contains: 'nlohmann/json\\\\.hpp'
    - path: "cmake/AssertHttpServerLocalhostOnly.cmake"
      provides: "lint asserting bind_to_port + 127.0.0.1 literal in driver/src/http_server.{hpp,cpp}; rejects 0.0.0.0 and INADDR_ANY"
      contains: '127\\\\.0\\\\.0\\\\.1'
    - path: "cmake/AssertHttpServerNoVrApi.cmake"
      provides: "sibling of AssertDetectionRunnerNoVrApi limited to http_server.{hpp,cpp}"
      contains: 'openvr'
    - path: "cmake/AssertNoConfigWriteInClient.cmake"
      provides: "GLOB_RECURSE lint over apps/micmap/ + src/steamvr/ that rejects saveDefault() / writeAtomicWindows / ReplaceFileW"
      contains: 'saveDefault'
    - path: "tests/CMakeLists.txt"
      provides: "ctest registrations for 4 new lints + 16 RED-tolerant test scaffolds"
      contains: "AssertHttpServerNoVrApi"
  key_links:
    - from: "tests/CMakeLists.txt"
      to: "cmake/AssertNoJsonInCore.cmake"
      via: "add_test NAME AssertNoJsonInCore COMMAND ${CMAKE_COMMAND} -P"
      pattern: "AssertNoJsonInCore"
    - from: "tests/CMakeLists.txt"
      to: "cmake/AssertHttpServerLocalhostOnly.cmake"
      via: "add_test NAME AssertHttpServerLocalhostOnly COMMAND ${CMAKE_COMMAND} -DHTTP_SERVER_DIR=... -P"
      pattern: "AssertHttpServerLocalhostOnly"
    - from: "tests/CMakeLists.txt"
      to: "tests/driver/put_settings_round_trip_test.cpp"
      via: "add_executable + EXISTS-gated source list pattern from P7 lines 197-214"
      pattern: 'EXISTS.*driver/src/config_io\\.cpp'
---

<objective>
Wave 0 RED-tolerant prerequisite for Phase 8. Land all 4 new CMake source-grep lints and all 16 GoogleTest-style plain-main test scaffolds before any production code lands. All scaffolds are written to the proven P6/P7 RED-tolerant pattern: lints skip on NOT-EXISTS; test exes use EXISTS-gated source lists so cmake configure stays green even though the impl files do not yet exist.

Purpose: This is the Nyquist gate for every subsequent plan. Build-time compile failures of these scaffolds (when an impl file does not exist or has the wrong shape) are the canonical "RED" signal that drives downstream development. The 4 lints become structural guardrails: AssertNoJsonInCore, AssertHttpServerLocalhostOnly, AssertHttpServerNoVrApi, AssertNoConfigWriteInClient. Output: 4 new lint scripts under cmake/, 16 new test scaffold files under tests/, all wired into tests/CMakeLists.txt with EXISTS-gated source lists.
</objective>

<execution_context>
@$HOME/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ipc-contract-reshape/08-CONTEXT.md
@.planning/phases/08-ipc-contract-reshape/08-PATTERNS.md
@.planning/phases/08-ipc-contract-reshape/08-VALIDATION.md
@cmake/lint_no_openvr_in_core.cmake
@cmake/AssertDetectionRunnerNoVrApi.cmake
@tests/CMakeLists.txt
@tests/driver/detection_settings_propagation_test.cpp

<interfaces>
<!-- Existing analog patterns to copy. Executor uses these directly without additional codebase exploration. -->

Existing lint regex set (from cmake/lint_no_openvr_in_core.cmake:42-44):
```cmake
if(_content MATCHES "[<\"]openvr[a-z_]*\\.h[>\"]"
        OR _content MATCHES "[^a-zA-Z0-9_]vr::"
        OR _content MATCHES "^vr::")
```

Existing three-file explicit list lint pattern (cmake/AssertDetectionRunnerNoVrApi.cmake:36-39):
```cmake
set(_targets
    "${DETECTION_RUNNER_DIR}/detection_runner.hpp"
    "${DETECTION_RUNNER_DIR}/detection_runner.cpp"
    "${DETECTION_RUNNER_DIR}/sample_ring.hpp")
```

Existing skip-on-NOT-EXISTS RED-tolerant branch (AssertDetectionRunnerNoVrApi.cmake:48-56):
```cmake
foreach(_file ${_targets})
    if(NOT EXISTS "${_file}")
        continue()
    endif()
    math(EXPR _files_scanned "${_files_scanned} + 1")
    file(READ "${_file}" _content)
    # ...regex check
endforeach()
```

Existing plain-main test convention (tests/driver/detection_settings_propagation_test.cpp:30-32):
```cpp
#define MM_CHECK(expr) do { if (!(expr)) { \
    std::cerr << "FAIL: " << #expr << " at line " << __LINE__ << "\n"; \
    return 1; } } while(0)
```

Existing EXISTS-gated source-list registration (tests/CMakeLists.txt:197-214):
```cmake
set(_p7_propagation_sources driver/detection_settings_propagation_test.cpp)
if(EXISTS "${CMAKE_SOURCE_DIR}/driver/src/detection_runner.cpp")
    list(APPEND _p7_propagation_sources
        "${CMAKE_SOURCE_DIR}/driver/src/detection_runner.cpp")
endif()
if(OpenVR_FOUND)
    add_executable(test_detection_settings_propagation ${_p7_propagation_sources})
    target_compile_features(test_detection_settings_propagation PRIVATE cxx_std_17)
    target_include_directories(test_detection_settings_propagation PRIVATE
        ${CMAKE_SOURCE_DIR}/driver/src)
    target_link_libraries(test_detection_settings_propagation PRIVATE
        micmap::core_runtime
        OpenVR::openvr_api)
    add_test(NAME DetectionSettingsPropagation
        COMMAND test_detection_settings_propagation)
endif()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land 4 new CMake source-grep lints under cmake/</name>
  <files>cmake/AssertNoJsonInCore.cmake, cmake/AssertHttpServerLocalhostOnly.cmake, cmake/AssertHttpServerNoVrApi.cmake, cmake/AssertNoConfigWriteInClient.cmake</files>
  <read_first>
    - cmake/lint_no_openvr_in_core.cmake (verbatim shape for GLOB_RECURSE lints)
    - cmake/AssertDetectionRunnerNoVrApi.cmake (verbatim shape for explicit-target-list + skip-on-NOT-EXISTS lints)
    - .planning/phases/08-ipc-contract-reshape/08-PATTERNS.md sections "AssertNoJsonInCore", "AssertHttpServerLocalhostOnly", "AssertHttpServerNoVrApi", "AssertNoConfigWriteInClient"
  </read_first>
  <action>
Create 4 lint files. Use Write tool, never heredoc.

**1. cmake/AssertNoJsonInCore.cmake** — copy the verbatim shape of `cmake/lint_no_openvr_in_core.cmake` (GLOB_RECURSE form across SRC_ROOTS). Change two things:
  - Header banner: replace "Phase 5 / D-02" with "Phase 8 / D-02"; replace "OpenVR header" with "nlohmann/json header"; lint name `lint_no_openvr_in_core` → `AssertNoJsonInCore`.
  - Single regex condition (drop the 3 OpenVR ones): `if(_content MATCHES "[<\"]nlohmann/json\\.hpp[>\"]")`.
  - Final status message: `AssertNoJsonInCore: clean (${_files_scanned} files scanned across ${_rootcount} roots)`.
  - Final FATAL message: `AssertNoJsonInCore: ${_vcount} file(s) violate the no-json-in-shared-layer rule:` (per CONTEXT D-02).
  - Required arg: `SRC_ROOTS` (matches the parent lint convention).

**2. cmake/AssertHttpServerLocalhostOnly.cmake** — copy the verbatim shape of `cmake/AssertDetectionRunnerNoVrApi.cmake` (explicit-target-list, RED-tolerant skip-on-NOT-EXISTS). Change:
  - Required arg: `HTTP_SERVER_DIR` (replaces `DETECTION_RUNNER_DIR`).
  - `_targets` list = `${HTTP_SERVER_DIR}/http_server.hpp`, `${HTTP_SERVER_DIR}/http_server.cpp` (two files only, no third sample_ring sibling).
  - Header banner: "Phase 8 / IPC-07 / Pitfall 7: assert HTTP server binds 127.0.0.1 only — never 0.0.0.0 or INADDR_ANY."
  - Regex set replaces the OpenVR triple with these three (a violation if ANY matches):
      ```
      _content MATCHES "0\\.0\\.0\\.0"
      OR _content MATCHES "INADDR_ANY"
      OR (_content MATCHES "bind_to_port" AND NOT _content MATCHES "127\\.0\\.0\\.1")
      ```
  - FATAL message: `AssertHttpServerLocalhostOnly: ${_vcount} file(s) violate the localhost-only-bind rule (IPC-07 / Pitfall 7):`.
  - Final status message: `AssertHttpServerLocalhostOnly: clean (${_files_scanned} files scanned)`.

**3. cmake/AssertHttpServerNoVrApi.cmake** — copy the verbatim shape of `cmake/AssertDetectionRunnerNoVrApi.cmake`. Change:
  - Required arg: `HTTP_SERVER_DIR`.
  - `_targets` list = `${HTTP_SERVER_DIR}/http_server.hpp`, `${HTTP_SERVER_DIR}/http_server.cpp`.
  - Header banner: "Phase 8 / D-24 / SVR-05: assert no vr::* in driver/src/http_server.{hpp,cpp}. HTTP thread must never call OpenVR API (Pitfall 3)."
  - Regex set is identical to AssertDetectionRunnerNoVrApi (the existing 3 OpenVR regexes).
  - FATAL message: `AssertHttpServerNoVrApi: ${_vcount} file(s) violate the no-vr::-in-http-server rule (D-24 / SVR-05 / Pitfall 3):`.

**4. cmake/AssertNoConfigWriteInClient.cmake** — copy the verbatim shape of `cmake/lint_no_openvr_in_core.cmake` (GLOB_RECURSE form across CLIENT_ROOTS). Change:
  - Required arg: `CLIENT_ROOTS` (replaces `SRC_ROOTS`).
  - Header banner: "Phase 8 / IPC-05 / D-07: assert client TUs do not write config.json. Driver is sole writer; client edits flow through PUT /settings."
  - Regex set is the 3-condition disjunction:
      ```
      _content MATCHES "saveDefault\\(\\)"
      OR _content MATCHES "writeAtomicWindows"
      OR _content MATCHES "ReplaceFileW"
      ```
  - File extensions list: `*.cpp`, `*.hpp` (narrower than the parent .h/.hxx/.c/.cc/.cxx/.inc/.ipp set per CONTEXT — only client production code, not headers from external).
  - FATAL message: `AssertNoConfigWriteInClient: ${_vcount} file(s) violate the single-writer rule (IPC-05 / D-07):`.
  - Status message: `AssertNoConfigWriteInClient: clean (${_files_scanned} files scanned across ${_rootcount} roots)`.

All 4 lints use `message(FATAL_ERROR ...)` on violations and `message(STATUS ...)` on clean. All preserve the file-counting `_files_scanned` math.
  </action>
  <verify>
    <automated>cmake -P cmake/AssertNoJsonInCore.cmake -DSRC_ROOTS=src/audio 2>&1 | grep -E "FATAL|clean"; cmake -P cmake/AssertHttpServerLocalhostOnly.cmake -DHTTP_SERVER_DIR=driver/src 2>&1 | grep -E "FATAL|clean"; cmake -P cmake/AssertHttpServerNoVrApi.cmake -DHTTP_SERVER_DIR=driver/src 2>&1 | grep -E "FATAL|clean"; cmake -P cmake/AssertNoConfigWriteInClient.cmake -DCLIENT_ROOTS=apps/micmap 2>&1 | grep -E "FATAL|clean"</automated>
  </verify>
  <done>
    All 4 lint scripts exist under cmake/. Running each in cmake script-mode emits a "clean (N files scanned)" STATUS line. AssertHttpServerNoVrApi scans http_server.hpp + .cpp and stays GREEN (the existing v1.5 file already complies — it includes only command_queue.hpp + driver_log.hpp + httplib.h + nlohmann/json.hpp). AssertHttpServerLocalhostOnly stays GREEN (existing http_server.hpp ctor defaults to "127.0.0.1" string). AssertNoJsonInCore stays GREEN (no shared-lib TU includes nlohmann/json.hpp; only src/core/src/config_manager.cpp does, and config_manager.cpp lives at src/core/ not src/audio|src/detection|src/common — wait: it lives at src/core/src/. CONTEXT D-02 lists src/audio, src/detection, src/core, src/common as the scope, so config_manager.cpp WILL violate. This is the existing v1.5 reality the json deferral lifts in 08-02 — at Wave 0 the lint must NOT be wired with src/core in scope yet; instead, register the lint with all 4 dirs and accept it goes RED for src/core/src/config_manager.cpp. **CORRECTION:** at Wave 0 register the lint script files only — the ctest wiring waits until 08-02. Do not add an `add_test(NAME AssertNoJsonInCore ...)` in tests/CMakeLists.txt at Wave 0). AssertNoConfigWriteInClient currently FATALs (apps/micmap/main.cpp:498 calls `configManager->saveDefault()` — this is the existing v1.5 reality. Same logic: do not register `add_test(NAME AssertNoConfigWriteInClient ...)` at Wave 0; that wiring lands in 08-04 alongside the saveDefault deletion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Land 16 RED-tolerant test scaffolds</name>
  <files>tests/driver/get_state_shape_test.cpp, tests/driver/get_telemetry_level_test.cpp, tests/driver/get_devices_cache_test.cpp, tests/driver/get_settings_shape_test.cpp, tests/driver/put_settings_round_trip_test.cpp, tests/driver/put_settings_validation_test.cpp, tests/driver/put_settings_stress100_test.cpp, tests/driver/init_config_share_violation_test.cpp, tests/driver/state_clear_error_test.cpp, tests/driver/config_io_atomic_persist_test.cpp, tests/test_multi_sink_logger.cpp, tests/test_settings_validator.cpp, tests/test_client_driver_loaded_indicator.cpp, tests/test_client_level_meter_cadence.cpp</files>
  <read_first>
    - tests/driver/detection_settings_propagation_test.cpp (verbatim plain-main + MM_CHECK shape)
    - tests/driver/audio_worker_lifecycle_headless.cpp (multi-case plain-main shape)
    - .planning/phases/08-ipc-contract-reshape/08-PATTERNS.md section "tests/driver/get_state_shape_test.cpp ... and 7 sibling driver tests" + "Pattern F: Plain-main exit-code test convention"
  </read_first>
  <action>
Create 14 new test scaffold files (the "16" count includes 2 existing files that don't need recreating — file list above shows 14 new). All use the plain-main + MM_CHECK convention. All include the same banner block describing what they test and which plan turns RED→GREEN. None use GoogleTest (the project convention per PATTERNS.md Pattern F is plain-main exit-code testing even though `MICMAP_USE_GTEST` exists as an option).

For each file emit this exact banner (with placeholders replaced):

```cpp
/**
 * @file <basename>.cpp
 * @brief Phase 8 Wave 0 RED scaffold for <SHORT_DESC>.
 *
 * Convention: plain-main, exit 0 = pass, 1 = fail. Mirrors
 * tests/driver/detection_settings_propagation_test.cpp / audio_worker_lifecycle_headless.cpp.
 *
 * RED until Plan <UNBLOCKING_PLAN> lands <IMPL_FILES>. This translation unit
 * is intentionally fail-to-build at Wave 0 — that compile failure IS the
 * Nyquist gate.
 */
```

Then for each file write a stub `int main()` that #includes the future production header(s), exercises the public surface against MM_CHECK assertions, and returns 0 on success / 1 on failure. The `#include` paths cause the BUILD-time RED signal until the impl ships.

**File-by-file specifications:**

| File | SHORT_DESC | UNBLOCKING_PLAN | INCLUDES (RED hooks) | TEST CASES (MM_CHECK lines) |
|------|-----------|-----------------|----------------------|------------------------------|
| `tests/driver/get_state_shape_test.cpp` | GET /state JSON shape (IPC-01) | 08-03 | `"http_server.hpp"`, `"driver_state.hpp"`, `<nlohmann/json.hpp>`, `<httplib.h>` | spin up an in-process HttpServer with a stub `stateGetter` returning a known DriverState; HTTP GET /state via httplib::Client; parse response; assert keys: driver_loaded == true, steamvr_running == true, detection_state == "idle", last_trigger_at == nullptr, last_error == nullptr, audio_device_id, audio_device_state |
| `tests/driver/get_telemetry_level_test.cpp` | GET /telemetry/level returns RMS (IPC-02) | 08-03 | `"http_server.hpp"`, `<nlohmann/json.hpp>`, `<httplib.h>` | stub rmsGetter returns 0.5f; GET /telemetry/level; parse; assert `rms_normalized == 0.5f` and `dbfs` field present (numeric) |
| `tests/driver/get_devices_cache_test.cpp` | GET /devices honors 1 s cache (IPC-03 / D-17) | 08-03 | `"http_server.hpp"`, `<chrono>`, `<httplib.h>`, `<nlohmann/json.hpp>` | counter-incrementing deviceLister stub; first GET /devices → counter==1; immediate second GET → counter still 1 (cached); sleep 1100 ms; third GET → counter==2 (cache invalidated) |
| `tests/driver/get_settings_shape_test.cpp` | GET /settings returns AppConfig JSON (IPC-04) | 08-03 | `"http_server.hpp"`, `"micmap/core/config_manager.hpp"`, `<httplib.h>` | stub configGetter returns AppConfig with sensitivity=0.5f; GET /settings; parse; assert version, audio, detection.sensitivity == 0.5f, steamvr, training keys present |
| `tests/driver/put_settings_round_trip_test.cpp` | PUT /settings persists then GET reflects (IPC-04 / D-09) | 08-04 | `"http_server.hpp"`, `"config_io.hpp"`, `<httplib.h>`, `<filesystem>` | tmp dir; stub configMutator that calls saveConfigJson + atomic_store; PUT /settings with sensitivity=0.42f → 200; GET /settings → sensitivity == 0.42f; verify file contents on disk match |
| `tests/driver/put_settings_validation_test.cpp` | PUT /settings 400 on bad input (IPC-04 / D-14) | 08-04 | `"http_server.hpp"`, `"settings_validator.hpp"`, `<httplib.h>`, `<nlohmann/json.hpp>` | PUT with sensitivity=-1.0f → status 400; parse body; assert {"field":"detection.sensitivity","reason":"must be in [0.0, 1.0]; got -1.000000"} (or substring "must be in [0.0, 1.0]"); GET /settings unchanged (no partial mutation) |
| `tests/driver/put_settings_stress100_test.cpp` | 100 PUTs in tight loop, no leaks (D-28) | 08-04 | `"http_server.hpp"`, `"config_io.hpp"`, `<windows.h>`, `<httplib.h>` | call `GetProcessHandleCount(GetCurrentProcess(), &before)`; 100 sequential PUTs on a tmp config path; `GetProcessHandleCount(..., &after)`; assert `(after - before) < 10` (stable handle delta); final GET /settings parses cleanly |
| `tests/driver/init_config_share_violation_test.cpp` | Driver Init retries 3× on ERROR_SHARING_VIOLATION (D-10) | 08-02 | `"config_io.hpp"`, `<windows.h>`, `<filesystem>`, `<thread>` | open config.json with `CreateFileW(... ,0 /*share*/, ...)` to provoke SHARING_VIOLATION; `loadConfigJson(path, outCfg)` on a separate thread; close handle after 100 ms; assert loadConfigJson returned true and outCfg has defaults (3 attempts × 50 ms backoff = 150 ms total < 200 ms timeout) |
| `tests/driver/state_clear_error_test.cpp` | POST /state/clear-error sets last_error null (HEALTH-05 / D-16) | 08-04 | `"http_server.hpp"`, `<httplib.h>` | stub state with last_error="X"; GET /state → last_error == "X"; POST /state/clear-error → 200; GET /state → last_error == nullptr |
| `tests/driver/config_io_atomic_persist_test.cpp` | saveConfigJson uses ReplaceFileW atomic-swap (D-14) | 08-02 | `"config_io.hpp"`, `<filesystem>`, `<fstream>` | tmp dir; saveConfigJson(path, cfg1); saveConfigJson(path, cfg2); assert file exists; assert file content parses to cfg2; assert no `path.tmp` orphan beside it |
| `tests/test_multi_sink_logger.cpp` | MultiSinkLogger fans out to all sinks (LIB-04 / D-19) | 08-01 | `"micmap/common/logger.hpp"`, `"micmap/common/log_sink.hpp"`, `<sstream>` | construct two lambda-backed test sinks (each appending to a captured stringstream); `MultiSinkLogger ml({sink1, sink2})`; ml.log(LogLevel::Info, "hello"); MM_CHECK both stringstreams contain "hello" |
| `tests/test_settings_validator.cpp` | validateSettings catches range/enum violations (D-14 / D-15) | 08-04 | `"settings_validator.hpp"`, `"micmap/core/config_manager.hpp"` | 5 cases: (1) valid → nullopt; (2) sensitivity=-1.0 → field=="detection.sensitivity"; (3) cooldownMs=-100 → field=="detection.cooldownMs"; (4) fftSize=1000 → field=="detection.fftSize" (not power-of-2); (5) bufferSizeMs=0 → field=="audio.bufferSizeMs" |
| `tests/test_client_driver_loaded_indicator.cpp` | DriverApi::connect 3-state enum on ECONNREFUSED vs Read timeout (HEALTH-01 / Pitfall 6) | 08-01 | `"micmap/steamvr/driver_api.hpp"` | construct DriverApi pointing at unreachable host:port; assert connect() returns ConnectResult::NotFound (no driver running ≠ Connected ≠ Timeout). Comment-block clarifies: full ECONNREFUSED-vs-Timeout differentiation requires a running stub HTTP server held with `set_read_timeout(0)`; case (b) is documented but skipped at scaffold time — only NotFound is asserted. |
| `tests/test_client_level_meter_cadence.cpp` | Level meter polls /telemetry/level at 5 Hz visible / 0.5 Hz tray (HEALTH-06) | 08-05 | `"micmap/steamvr/driver_api.hpp"`, `<chrono>` | mock IsIconic state via a settable function pointer; over 1 s real time count getTelemetryLevel() invocations; visible mode: assert 4..6 calls; iconic mode: assert 0..1 calls. Timing-tolerant; documents intent only — RED until 08-05 lands the polling loop. |

For each file write the actual stub. Each main() must have at least 1 MM_CHECK and one PASS/SUCCESS stdout line; on success print `std::cout << "all tests passed\n"; return 0;` like the analog. For tests that need an HttpServer, construct an instance using a stub CommandQueue and stub callback functions — do NOT spin up a real network listener at Wave 0 (the scaffolds compile but may not connect; the RED gate is BUILD failure, not RUNTIME failure, per CONTEXT 08-VALIDATION.md).
  </action>
  <verify>
    <automated>for f in tests/driver/get_state_shape_test.cpp tests/driver/get_telemetry_level_test.cpp tests/driver/get_devices_cache_test.cpp tests/driver/get_settings_shape_test.cpp tests/driver/put_settings_round_trip_test.cpp tests/driver/put_settings_validation_test.cpp tests/driver/put_settings_stress100_test.cpp tests/driver/init_config_share_violation_test.cpp tests/driver/state_clear_error_test.cpp tests/driver/config_io_atomic_persist_test.cpp tests/test_multi_sink_logger.cpp tests/test_settings_validator.cpp tests/test_client_driver_loaded_indicator.cpp tests/test_client_level_meter_cadence.cpp; do test -f "$f" || { echo "MISSING $f"; exit 1; }; grep -q "MM_CHECK" "$f" || { echo "NO MM_CHECK in $f"; exit 1; }; grep -q "Phase 8 Wave 0 RED scaffold" "$f" || { echo "NO BANNER in $f"; exit 1; }; done; echo "all 14 scaffolds OK"</automated>
  </verify>
  <done>14 new scaffold files exist; each has MM_CHECK + Phase 8 Wave 0 RED scaffold banner; each #includes a header that does not yet exist (so build-time compile failure is the Nyquist gate per CONTEXT 08-VALIDATION.md). Existing tests/driver/audio_worker_lifecycle_headless.cpp and tests/driver/detection_settings_propagation_test.cpp remain untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Wire all lints + scaffolds into tests/CMakeLists.txt with EXISTS-gated source lists</name>
  <files>tests/CMakeLists.txt</files>
  <read_first>
    - tests/CMakeLists.txt (existing P5/P6/P7 wiring at lines 116-256)
    - .planning/phases/08-ipc-contract-reshape/08-PATTERNS.md "Pattern E: Wave 0 RED-tolerant ctest registration"
  </read_first>
  <action>
Append a new `# ---- Phase 8 Wave 0 (RED scaffold) ----` block to tests/CMakeLists.txt after the existing P7 block (after line 256). Use Edit tool. The block adds:

**A. Three new ctest lints** (NOT AssertNoJsonInCore and NOT AssertNoConfigWriteInClient — those wait until 08-02 and 08-04 respectively because they currently fire on existing v1.5 code):

```cmake
# ---- Phase 8 Wave 0 (RED scaffold) ----

# P8 D-25 / IPC-07 / Pitfall 7: assert HTTP server binds 127.0.0.1 only.
# Wave 0 RED-tolerant via skip-on-NOT-EXISTS in the lint script. Currently
# stays GREEN — existing http_server.hpp ctor defaults host to "127.0.0.1".
add_test(NAME AssertHttpServerLocalhostOnly
    COMMAND ${CMAKE_COMMAND}
        -DHTTP_SERVER_DIR=${CMAKE_SOURCE_DIR}/driver/src
        -P ${CMAKE_SOURCE_DIR}/cmake/AssertHttpServerLocalhostOnly.cmake)

# P8 D-24 / SVR-05 / Pitfall 3: assert no vr::* in http_server.{hpp,cpp}.
# Sibling of AssertDetectionRunnerNoVrApi. Currently GREEN — existing
# http_server.cpp includes only command_queue + driver_log + httplib + nlohmann.
add_test(NAME AssertHttpServerNoVrApi
    COMMAND ${CMAKE_COMMAND}
        -DHTTP_SERVER_DIR=${CMAKE_SOURCE_DIR}/driver/src
        -P ${CMAKE_SOURCE_DIR}/cmake/AssertHttpServerNoVrApi.cmake)
```

Do NOT register `AssertNoJsonInCore` or `AssertNoConfigWriteInClient` at Wave 0 — they currently fire on existing v1.5 reality (config_manager.cpp uses nlohmann/json; main.cpp:498 calls saveDefault). They get registered in plans 08-02 and 08-04 respectively, AFTER the violations are removed.

**B. Test-exe registrations** for the 14 driver/client scaffolds. Each follows the EXISTS-gated source-list pattern from tests/CMakeLists.txt:197-214. Use `if(OpenVR_FOUND)` for any test that pulls in driver TUs (which transitively include driver_log.hpp → openvr_driver.h). Test exes that are pure shared-lib do not need OpenVR_FOUND gate.

For each driver test exe, the EXISTS-gated impl-source extension list is (mapped to the impl files the test will pull in once they land):

| Test target | Impl files (EXISTS-gated APPEND) | OpenVR_FOUND gated | Extra link libs |
|-------------|----------------------------------|--------------------|------------------|
| test_get_state_shape | driver/src/http_server.cpp, driver/src/device_provider.cpp, driver/src/audio_worker.cpp, driver/src/detection_runner.cpp, driver/src/command_queue.hpp (header-only) | yes | micmap::core_runtime, micmap::bindings, httplib::httplib, OpenVR::openvr_api, nlohmann_json |
| test_get_telemetry_level | (same as above) | yes | (same) |
| test_get_devices_cache | (same as above) | yes | (same) |
| test_get_settings_shape | (same as above) + driver/src/config_io.cpp | yes | (same) |
| test_put_settings_round_trip | (same as above) + driver/src/config_io.cpp + driver/src/settings_validator.cpp | yes | (same) |
| test_put_settings_validation | driver/src/settings_validator.cpp + http_server.cpp + (same driver TUs as get_state) | yes | (same) |
| test_put_settings_stress100 | (same as round_trip) | yes | (same) |
| test_init_config_share_violation | driver/src/config_io.cpp + driver/src/device_provider.cpp + (same driver TUs) | yes | (same) |
| test_state_clear_error | driver/src/http_server.cpp + (same driver TUs) | yes | (same) |
| test_config_io_atomic_persist | driver/src/config_io.cpp (PURE — no other driver TUs) | NO (no OpenVR via config_io alone) | micmap::core_runtime, nlohmann_json |
| test_multi_sink_logger | src/common/src/multi_sink_logger.cpp + src/common/src/sinks/file_log_sink.cpp + src/common/src/sinks/stdout_log_sink.cpp | NO | micmap::core_runtime |
| test_settings_validator | driver/src/settings_validator.cpp | NO | micmap::core_runtime |
| test_client_driver_loaded_indicator | (links via micmap::steamvr — uses post-rename driver_api.cpp) | NO (steamvr pulls openvr but it's already linked) | micmap::steamvr |
| test_client_level_meter_cadence | (links via micmap::steamvr) | NO | micmap::steamvr |

For each entry: write the `set(_p8_<name>_sources tests/driver/<name>_test.cpp)` (or `tests/<name>.cpp` for non-driver tests), then a chain of `if(EXISTS "${CMAKE_SOURCE_DIR}/<impl>") list(APPEND ...)`, then the `if(OpenVR_FOUND)` gate (when applicable), `add_executable`, `target_compile_features(... PRIVATE cxx_std_17)`, `target_include_directories(... PRIVATE ${CMAKE_SOURCE_DIR}/driver/src)` (where applicable), `target_link_libraries(... PRIVATE ...)`, `target_compile_definitions(... PRIVATE CPPHTTPLIB_NO_EXCEPTIONS)` (where httplib::httplib is linked), and `add_test(NAME <PascalCaseName> COMMAND <test_exe>)`. Test names: GetStateShape, GetTelemetryLevel, GetDevicesCache, GetSettingsShape, PutSettingsRoundTrip, PutSettingsValidation, PutSettingsStress100, InitConfigShareViolation, StateClearError, ConfigIoAtomicPersist, MultiSinkLogger, SettingsValidator, ClientDriverLoadedIndicator, ClientLevelMeterCadence.

Mirror the existing `else() message(STATUS "<TestName>: skipped (OpenVR SDK not found)") endif()` branch from tests/CMakeLists.txt:177-179 for the OpenVR_FOUND-gated registrations.

Wrap the entire block under one `# ---- Phase 8 Wave 0 (RED scaffold) ----` separator. Total addition: ~150-200 lines of CMake.
  </action>
  <verify>
    <automated>cmake -S . -B build-headless -DMICMAP_BUILD_DRIVER=OFF 2>&1 | tail -30; cmake -B build-headless --build 2>&1 | head -5; cd build-headless && ctest -N -R "AssertHttpServer" 2>&1 | grep -E "Test"</automated>
  </verify>
  <done>tests/CMakeLists.txt parses cleanly under cmake configure (no FATAL_ERROR); `ctest -N` lists `AssertHttpServerLocalhostOnly` and `AssertHttpServerNoVrApi`; the 14 test scaffold registrations appear (most as "skipped (OpenVR SDK not found)" under -DMICMAP_BUILD_DRIVER=OFF — that's correct). When OpenVR is present, configure must still succeed (the EXISTS-gated source lists keep the impl-less scaffolds compiling against just the test TU until 08-01..08-04 land their impls).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CMake → ctest | lint scripts run as cmake script-mode (-P); pure read-only file scans. No untrusted input crosses this boundary at Wave 0. |
| Wave 0 test scaffolds → driver impl | scaffolds are intentionally fail-to-build until impl ships; no runtime trust boundary at Wave 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | T (Tampering) | lint scripts | mitigate | Lint scripts are committed to git under cmake/; CI checks the lint passes on every push. Tampering with the lint to skip violations would show up in PR review. |
| T-08-00-02 | I (Information disclosure) | test scaffolds | accept | Test scaffolds may print MM_CHECK FAIL lines including local file paths and field names; risk is local-only (developer machine). No PII, no production secrets. |
| T-08-00-03 | D (Denial of service) | ctest configure | accept | RED-tolerant gates ensure cmake configure stays green even with all 14 scaffolds present pre-impl. A misconfigured EXISTS-gate would only slow developer feedback, not crash CI. |
</threat_model>

<verification>
**Wave 0 completion verifies via:**
1. `cmake -S . -B build && cmake --build build` succeeds (no compile failures, no FATAL configure)
2. `ctest -N --test-dir build` lists at minimum: AssertHttpServerLocalhostOnly, AssertHttpServerNoVrApi (and the 14 scaffold registrations as either present-or-skipped)
3. `ctest -R "AssertHttpServer" --test-dir build` runs both lints; both PASS (existing http_server.* already complies)
4. `find tests/ -name "*_test.cpp" -newer tests/test_placeholder.cpp -print | wc -l` shows the 14 new scaffold files
5. `find cmake/ -name "Assert*.cmake" -newer cmake/AssertNoOpenVRInCore.cmake | wc -l` shows the 4 new lint files (3 of which are P8-new; one is `AssertNoJsonInCore`)

**Note on lint-not-yet-wired:** AssertNoJsonInCore and AssertNoConfigWriteInClient lint SCRIPTS exist after Wave 0 but are NOT yet registered as ctests. Plans 08-02 and 08-04 register them once their respective violations are removed. This is intentional per CONTEXT D-29 ordering — registering them at Wave 0 would produce immediate FATAL on the existing v1.5 reality.
</verification>

<success_criteria>
- [ ] 4 new lint scripts under cmake/ (AssertNoJsonInCore, AssertHttpServerLocalhostOnly, AssertHttpServerNoVrApi, AssertNoConfigWriteInClient)
- [ ] 14 new test scaffold files (`tests/driver/*_test.cpp` and `tests/test_*.cpp`) all containing the Phase 8 Wave 0 RED scaffold banner + at least one MM_CHECK
- [ ] tests/CMakeLists.txt extended with `# ---- Phase 8 Wave 0 (RED scaffold) ----` block registering AssertHttpServerLocalhostOnly + AssertHttpServerNoVrApi as ctests + 14 test-exe registrations under EXISTS-gated source lists
- [ ] `cmake -S . -B build-headless -DMICMAP_BUILD_DRIVER=OFF` configures cleanly (no FATAL)
- [ ] `ctest -R "AssertHttpServer" --test-dir build` runs and PASSES (both lints clean against existing v1.5 source)
- [ ] No regression: existing tests/CMakeLists.txt entries (test_placeholder, test_config_manager, test_command_queue, test_cli_flags_parse, test_manifest_registrar, test_vr_input_quit_ordering, test_tray_balloon_once, test_vrmanifest_schema, test_bindings_patcher, lint_no_openvr_in_core, lint_no_driver_macro, AudioWorkerLifecycleHeadless, AssertAudioWorkerNoVrApi, DetectionSettingsPropagation, DeviceProviderLifecycleStress, AssertDetectionRunnerNoVrApi) all still register and pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-ipc-contract-reshape/08-00-SUMMARY.md` summarizing: 4 lint scripts created, 14 test scaffolds created, 16 ctest registrations added (2 lints + 14 test exes; AssertNoJsonInCore + AssertNoConfigWriteInClient deliberately deferred to 08-02 / 08-04 wiring). List the EXISTS-gate impl files each scaffold awaits.
</output>
