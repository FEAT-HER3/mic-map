---
phase: 05-shared-library-extraction
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/mic_test/CMakeLists.txt
  - apps/micmap/CMakeLists.txt
autonomous: false
requirements: [LIB-02]
must_haves:
  truths:
    - "apps/mic_test/CMakeLists.txt links exactly micmap::core_runtime (replacing the prior 3-lib enum micmap_audio + micmap_detection + micmap_common) per D-08"
    - "apps/micmap/CMakeLists.txt links micmap::core_runtime AND keeps a direct micmap_steamvr link per D-09"
    - "apps/micmap/CMakeLists.txt no longer references micmap_lib or micmap::lib"
    - "apps/hmd_button_test/CMakeLists.txt is unchanged (still links micmap_steamvr direct)"
    - "Full driver-on build matrix succeeds: cmake -B build && cmake --build build --config Release produces driver_micmap.dll, micmap.exe, mic_test.exe; ctest is green"
    - "Headless build matrix succeeds: cmake -B build-headless -DMICMAP_BUILD_DRIVER=OFF && cmake --build build-headless --target mic_test --config Release succeeds without OpenVR present (SC1)"
    - "Manual UAT: 5-cycle SteamVR start/stop on Bigscreen Beyond rig with v1.5 trigger path still working (SC5 — visual confirmation that driver behavior is byte-for-byte identical to v1.5)"
  artifacts:
    - path: "apps/mic_test/CMakeLists.txt"
      provides: "PRIVATE link of micmap::core_runtime (D-08, SC1 acceptance probe — replaces micmap_audio + micmap_detection + micmap_common)"
      contains: "micmap::core_runtime"
    - path: "apps/micmap/CMakeLists.txt"
      provides: "PRIVATE link of micmap::core_runtime alongside direct micmap_steamvr (D-09); micmap_lib reference removed"
      contains: "micmap::core_runtime"
  key_links:
    - from: "apps/mic_test/CMakeLists.txt"
      to: "micmap::core_runtime"
      via: "target_link_libraries(mic_test PRIVATE ...)"
      pattern: "target_link_libraries\\(mic_test[\\s\\S]*?micmap::core_runtime"
    - from: "apps/micmap/CMakeLists.txt"
      to: "micmap::core_runtime + micmap_steamvr (both PRIVATE; both present)"
      via: "target_link_libraries(micmap PRIVATE ...)"
      pattern: "target_link_libraries\\(micmap[\\s\\S]*?micmap::core_runtime[\\s\\S]*?micmap_steamvr"
---

<objective>
Finish Phase 5 Wave 2: relink the two app consumers (`mic_test.exe` and `micmap.exe`) onto `micmap::core_runtime`, then run the full build matrix (driver-on + headless) and the manual SC5 UAT on the Bigscreen Beyond rig. After this plan, all five Phase 5 success criteria are satisfied: the runtime aggregation exists (LIB-01, Plan 05-02), all three binaries link it (LIB-02, this plan), the configure-time guard fails the build on OpenVR leaks (LIB-03, Plan 05-01), driver export surface is unchanged (SC3), MICMAP_DRIVER_BUILD remains zero in the shared layer (SC4), and v1.5 driver behavior is byte-for-byte preserved (SC5 manual UAT).

Purpose: SC1 — `mic_test.exe` MUST build with `-DMICMAP_BUILD_DRIVER=OFF` and OpenVR SDK absent. The new `micmap::core_runtime` link on `mic_test` is the single acceptance probe that proves the headless invariant is preserved end-to-end. SC5 — the v1.5 trigger path through `POST /button` MUST still work on the Bigscreen Beyond rig because no driver TU was changed in Plan 05-02.

Output: Two modified CMakeLists.txt files; one passing driver-on build matrix; one passing headless build matrix; one signed-off manual SC5 UAT.
</objective>

<execution_context>
@$HOME/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-shared-library-extraction/05-CONTEXT.md
@.planning/phases/05-shared-library-extraction/05-RESEARCH.md
@.planning/phases/05-shared-library-extraction/05-PATTERNS.md
@.planning/phases/05-shared-library-extraction/05-VALIDATION.md
@.planning/phases/05-shared-library-extraction/05-01-SUMMARY.md
@apps/mic_test/CMakeLists.txt
@apps/micmap/CMakeLists.txt
@apps/hmd_button_test/CMakeLists.txt
@CMakeLists.txt

<interfaces>
<!-- Existing app link-list shapes that this plan modifies. -->
<!-- Executor must mirror Phase 4 D-10 PRIVATE alias-style precedent. -->

apps/mic_test/CMakeLists.txt — current target_link_libraries (lines 8-13):
```cmake
target_link_libraries(mic_test
    PRIVATE
        micmap_audio
        micmap_detection
        micmap_common
)
```
Other lines (cxx_std_17 PRIVATE, WIN32 comctl32+comdlg32 block, RUNTIME_OUTPUT_DIRECTORY)
are unchanged.

apps/micmap/CMakeLists.txt — current target_link_libraries (lines 39-44):
```cmake
target_link_libraries(micmap
    PRIVATE
        micmap_lib       # <-- to be replaced (D-09)
        imgui
        micmap::bindings
)
```
The vrmanifest configure_file block (lines 11-29), POST_BUILD copies (lines 66-82),
install rules (lines 87-88), and Pathcch/shell32/dwmapi WIN32 block (lines 55-57)
are unchanged.

apps/hmd_button_test/CMakeLists.txt — DELIBERATELY NOT MODIFIED.
RESEARCH.md line 163 + PATTERNS.md line 16 confirm this file is the VR-only
harness and continues to link micmap_steamvr direct. No changes here.

Headless invariant (root CMakeLists.txt:96-102 — already in place):
```cmake
if(MICMAP_BUILD_DRIVER)
    if(OpenVR_FOUND)
        add_subdirectory(driver)
    else()
        message(STATUS "Skipping driver build - OpenVR SDK not found")
    endif()
endif()
```
Means -DMICMAP_BUILD_DRIVER=OFF skips the driver subdir entirely. mic_test must
still link cleanly because its only deps are the four OpenVR-free sub-libs
(via micmap::core_runtime) plus comctl32/comdlg32.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Relink apps/mic_test and apps/micmap onto micmap::core_runtime</name>
  <files>apps/mic_test/CMakeLists.txt, apps/micmap/CMakeLists.txt</files>
  <read_first>
    - apps/mic_test/CMakeLists.txt (current 3-lib link enum at lines 8-13)
    - apps/micmap/CMakeLists.txt (current micmap_lib reference at line 41; unchanged blocks: vrmanifest configure_file, POST_BUILD, install rules)
    - apps/hmd_button_test/CMakeLists.txt (READ to confirm: this file is NOT modified — it is the analog showing the unchanged-link pattern)
    - .planning/phases/05-shared-library-extraction/05-CONTEXT.md (D-08 — mic_test gets single PRIVATE micmap::core_runtime; D-09 — micmap keeps micmap_steamvr direct alongside the new aggregate)
    - .planning/phases/05-shared-library-extraction/05-PATTERNS.md (apps/mic_test/CMakeLists.txt and apps/micmap/CMakeLists.txt pattern assignments — exact replacement shapes)
    - .planning/phases/05-shared-library-extraction/05-RESEARCH.md (Code Examples §Pattern 2 — "AFTER" snippets for both apps)
  </read_first>
  <behavior>
    - In `apps/mic_test/CMakeLists.txt`: replace the three sub-lib lines (`micmap_audio`, `micmap_detection`, `micmap_common`) inside `target_link_libraries(mic_test PRIVATE ...)` with a single `micmap::core_runtime` line. Keep `target_compile_features(mic_test PRIVATE cxx_std_17)`, the WIN32 `comctl32 comdlg32` block, and the `RUNTIME_OUTPUT_DIRECTORY` set_target_properties block exactly as-is.
    - In `apps/micmap/CMakeLists.txt`: replace `micmap_lib` (line 41) with `micmap::core_runtime` AND `micmap_steamvr`. The new link list is, in order: `micmap::core_runtime`, `micmap_steamvr`, `imgui`, `micmap::bindings`. Keep all other content of `apps/micmap/CMakeLists.txt` byte-identical (vrmanifest configure_file, POST_BUILD copies, install rules, WIN32 shell32/dwmapi/Pathcch block, target_compile_features).
    - Do NOT modify `apps/hmd_button_test/CMakeLists.txt` — it is the VR-only harness that continues to link `micmap_steamvr` direct.
    - After this task, `grep -rn 'micmap_lib\|micmap::lib' apps/ driver/ tests/ src/` must return zero hits.
  </behavior>
  <action>
**File 1: `apps/mic_test/CMakeLists.txt`** — replace the link block at lines 8-13.

CURRENT (lines 8-13):
```cmake
target_link_libraries(mic_test
    PRIVATE
        micmap_audio
        micmap_detection
        micmap_common
)
```

REPLACEMENT (verbatim):
```cmake
# Phase 5 D-08 (SC1 acceptance probe): mic_test.exe links the new shared
# runtime aggregate exclusively. The previous three-lib enum (micmap_audio,
# micmap_detection, micmap_common) is replaced by a single micmap::core_runtime
# reference. mic_test.exe MUST continue to build with -DMICMAP_BUILD_DRIVER=OFF
# and OpenVR SDK absent — this link is the single point of failure for that
# headless invariant.
target_link_libraries(mic_test
    PRIVATE
        micmap::core_runtime
)
```

Lines 1-6 (header + add_executable), line 15 (target_compile_features cxx_std_17), lines 17-20 (WIN32 comctl32+comdlg32 block), and lines 22-25 (RUNTIME_OUTPUT_DIRECTORY set_target_properties) are unchanged.

**File 2: `apps/micmap/CMakeLists.txt`** — replace the link block at lines 39-44.

CURRENT (lines 39-44):
```cmake
target_link_libraries(micmap
    PRIVATE
        micmap_lib
        imgui
        micmap::bindings
)
```

REPLACEMENT (verbatim):
```cmake
# Phase 5 D-09: micmap.exe links the new shared runtime aggregate AND keeps
# a direct micmap_steamvr link. The steamvr lib carries IDriverClient + the
# HTTP-bridge plumbing that apps/micmap/main.cpp still calls — that lib lives
# OUTSIDE the shared runtime (D-05) because it links OpenVR PUBLIC, so it must
# remain a direct app-level dependency until later v1.6 phases lift the trigger
# path into the driver. The old micmap_lib aggregator is deleted (D-07).
target_link_libraries(micmap
    PRIVATE
        micmap::core_runtime
        micmap_steamvr
        imgui
        micmap::bindings
)
```

All other lines in `apps/micmap/CMakeLists.txt` are byte-identical: the MICMAP_SOURCES set, the vrmanifest variables and configure_file block (lines 22-29), the add_executable (lines 35-37), the target_compile_features (line 46), the target_include_directories (line 49), the WIN32 shell32/dwmapi/Pathcch block (lines 55-57), the RUNTIME_OUTPUT_DIRECTORY set_target_properties (lines 60-62), the POST_BUILD app.vrmanifest copy (lines 66-73), the POST_BUILD config-file copy (lines 76-82), and the install(FILES ... app.vrmanifest) (lines 87-88).

**Do NOT modify `apps/hmd_button_test/CMakeLists.txt`.** Confirm the file is unchanged at end of task.
  </action>
  <verify>
    <automated>awk '/target_link_libraries\(mic_test/,/\)/' apps/mic_test/CMakeLists.txt | grep -q "micmap::core_runtime" &amp;&amp; ! awk '/target_link_libraries\(mic_test/,/\)/' apps/mic_test/CMakeLists.txt | grep -qE "micmap_audio|micmap_detection|micmap_common" &amp;&amp; awk '/target_link_libraries\(micmap/,/\)/' apps/micmap/CMakeLists.txt | grep -q "micmap::core_runtime" &amp;&amp; awk '/target_link_libraries\(micmap/,/\)/' apps/micmap/CMakeLists.txt | grep -q "micmap_steamvr" &amp;&amp; ! grep -rn "micmap_lib\|micmap::lib" apps/ driver/ tests/ src/ &amp;&amp; git diff --quiet -- apps/hmd_button_test/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mic_test/CMakeLists.txt` `target_link_libraries(mic_test PRIVATE ...)` block contains `micmap::core_runtime` (verified by `awk` block extract + `grep -q`).
    - `apps/mic_test/CMakeLists.txt` `target_link_libraries(mic_test PRIVATE ...)` block does NOT contain `micmap_audio`, `micmap_detection`, or `micmap_common` (verified by negated `awk + grep -qE`).
    - `apps/micmap/CMakeLists.txt` `target_link_libraries(micmap PRIVATE ...)` block contains all four of: `micmap::core_runtime`, `micmap_steamvr`, `imgui`, `micmap::bindings`.
    - `! grep -rn "micmap_lib\|micmap::lib" apps/ driver/ tests/ src/` exits 0 — D-07 invariant: no consumer references the deleted aggregator anywhere in the source tree.
    - `apps/hmd_button_test/CMakeLists.txt` is unchanged from the pre-plan baseline (verify with `git diff --quiet -- apps/hmd_button_test/CMakeLists.txt` exits 0).
    - The unchanged blocks of `apps/micmap/CMakeLists.txt` are still present: `grep -q "configure_file" apps/micmap/CMakeLists.txt` exits 0, `grep -q "app.vrmanifest" apps/micmap/CMakeLists.txt` exits 0, `grep -q "Pathcch" apps/micmap/CMakeLists.txt` exits 0.
  </acceptance_criteria>
  <done>Both app CMakeLists.txt files updated per D-08 and D-09; hmd_button_test is byte-identical; no reference to micmap_lib or micmap::lib survives anywhere in the tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full build matrix (driver-on + headless) and SC4 grep audit</name>
  <files>(no source changes — verification + matrix execution)</files>
  <read_first>
    - .planning/phases/05-shared-library-extraction/05-RESEARCH.md (Code Examples §Example 3 — headless build verification recipe; §Example 5 — SC4 grep recipe; Validation Architecture table — phase requirements → test map)
    - .planning/phases/05-shared-library-extraction/05-VALIDATION.md (sampling rate: per-wave full configure + build matrix + ctest)
    - .planning/phases/05-shared-library-extraction/05-CONTEXT.md (D-12 — MICMAP_DRIVER_BUILD zero-hit invariant; D-15 — cpp-httplib bump deferred; D-16 — json centralization deferred)
  </read_first>
  <behavior>
    - Run the **driver-on** matrix from a clean build directory: `cmake -B build && cmake --build build --config Release`. This builds `driver_micmap.dll`, `micmap.exe`, `mic_test.exe`, all tests, and runs the AssertNoOpenVRInCore guard at configure time.
    - Run `ctest --test-dir build -C Release --output-on-failure` to exercise both new lints (`lint_no_openvr_in_core`, `lint_no_driver_macro`) plus all pre-existing tests; full suite must be green.
    - Run the **headless** matrix from a separate clean build directory, with the OpenVR SDK temporarily moved aside (per RESEARCH.md Example 3): rename `external/openvr` to `external/openvr.bak` (if present), unset `OPENVR_SDK_PATH`, `cmake -B build-headless -DMICMAP_BUILD_DRIVER=OFF -G "Visual Studio 17 2022" -A x64`, then `cmake --build build-headless --target mic_test --config Release`. Restore `external/openvr.bak` to `external/openvr` after. mic_test must build cleanly without OpenVR present (SC1).
    - Re-run the SC4 grep manually as a phase-gate check: `grep -rn 'MICMAP_DRIVER_BUILD' src/audio/ src/detection/ src/core/ src/common/` must return zero matches (CTest already exercises this via `lint_no_driver_macro`, but record it explicitly in the SUMMARY).
    - Confirm `dumpbin /exports build/driver/micmap/bin/win64/driver_micmap.dll` still shows exactly one symbol `HmdDriverFactory` (SC3, unchanged from Plan 05-02 Task 2 acceptance — re-run to record post-app-relink state).
    - Capture binary-size deltas: list sizes of `build/bin/Release/mic_test.exe`, `build/bin/Release/micmap.exe`, `build/driver/micmap/bin/win64/driver_micmap.dll` and record in SUMMARY (Pitfall 5-D early warning; expected deltas are near-zero since no source changed).
  </behavior>
  <action>
This task does not modify source files. It executes the build/test matrix and records evidence.

**Step 1 — Driver-on build matrix (must pass):**
```bash
rm -rf build
cmake -B build -G "Visual Studio 17 2022" -A x64
cmake --build build --config Release
ctest --test-dir build -C Release --output-on-failure
```
Expected: configure succeeds with `STATUS: AssertNoOpenVRInCore: clean (visited N targets)`; full build produces `driver_micmap.dll`, `micmap.exe`, `mic_test.exe`; full ctest is green including `lint_no_openvr_in_core` and `lint_no_driver_macro`.

**Step 2 — Headless build matrix (must pass) per RESEARCH.md Example 3:**
```bash
# Move external/openvr aside if present so find_package(OpenVR QUIET) returns NOT FOUND.
if [ -d external/openvr ]; then mv external/openvr external/openvr.bak; fi
unset OPENVR_SDK_PATH

rm -rf build-headless
cmake -B build-headless -DMICMAP_BUILD_DRIVER=OFF -G "Visual Studio 17 2022" -A x64
cmake --build build-headless --target mic_test --config Release

# Restore.
if [ -d external/openvr.bak ]; then mv external/openvr.bak external/openvr; fi
```
Expected: configure prints `OpenVR not found - SteamVR features will use stub implementation` (root CMakeLists.txt:73); the `MICMAP_BUILD_DRIVER=OFF` gate skips `add_subdirectory(driver)` (root CMakeLists.txt:96-102); `mic_test` target builds cleanly; the AssertNoOpenVRInCore guard runs against `micmap_core_runtime` and reports clean. **SC1 PASSES.**

**Step 3 — SC4 explicit grep (CTest already covers this; record manually):**
```bash
! grep -rn 'MICMAP_DRIVER_BUILD' src/audio/ src/detection/ src/core/ src/common/
echo "SC4 grep exit code: $?"   # Must print 0 (no matches → grep returns 1 → ! flips to 0)
```

**Step 4 — SC3 dumpbin (MSVC Developer Command Prompt):**
```
dumpbin /exports build\driver\micmap\bin\win64\driver_micmap.dll
```
Expected: exactly one entry `HmdDriverFactory`. Record full output in SUMMARY.

**Step 5 — Binary size deltas (Pitfall 5-D early warning):**
List sizes of `build/bin/Release/mic_test.exe`, `build/bin/Release/micmap.exe`, `build/driver/micmap/bin/win64/driver_micmap.dll`. Compare against pre-Phase-5 baseline (recorded in `.planning/STATE.md` if available, otherwise document as the post-Phase-5 baseline for future reference). Expected delta: at most a few KB on each binary. A larger delta on `driver_micmap.dll` specifically would suggest an accidental driver-TU runtime-header include (Pitfall 5-D).

If any matrix step fails, stop and report the failure. Do not proceed to Task 3 (manual UAT) until both build matrices and the full ctest run are green.
  </action>
  <verify>
    <automated>rm -rf build &amp;&amp; cmake -B build -G "Visual Studio 17 2022" -A x64 2>build-cfg.log &amp;&amp; grep -q "AssertNoOpenVRInCore: clean" build-cfg.log &amp;&amp; cmake --build build --config Release &amp;&amp; ctest --test-dir build -C Release --output-on-failure &amp;&amp; ! grep -rn 'MICMAP_DRIVER_BUILD' src/audio/ src/detection/ src/core/ src/common/ &amp;&amp; (if [ -d external/openvr ]; then mv external/openvr external/openvr.bak; fi) &amp;&amp; rm -rf build-headless &amp;&amp; cmake -B build-headless -DMICMAP_BUILD_DRIVER=OFF -G "Visual Studio 17 2022" -A x64 2>build-headless-cfg.log &amp;&amp; grep -q "AssertNoOpenVRInCore: clean" build-headless-cfg.log &amp;&amp; cmake --build build-headless --target mic_test --config Release; rc=$?; if [ -d external/openvr.bak ]; then mv external/openvr.bak external/openvr; fi; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - Driver-on `cmake -B build` configure log contains `STATUS: AssertNoOpenVRInCore: clean (visited N targets)` (positive proof the runtime target exists and the guard walked it).
    - `cmake --build build --config Release` exits 0 and produces all three binaries: `build/driver/micmap/bin/win64/driver_micmap.dll`, `build/bin/Release/micmap.exe`, `build/bin/Release/mic_test.exe`.
    - `ctest --test-dir build -C Release --output-on-failure` exits 0 with both new lints (`lint_no_openvr_in_core`, `lint_no_driver_macro`) listed in the green output, plus all pre-existing tests (test_placeholder, test_config_manager, test_command_queue, test_cli_flags_parse, test_manifest_registrar, test_vr_input_quit_ordering, test_tray_balloon_once, test_vrmanifest_schema, test_bindings_patcher, bindings_patcher_idempotent).
    - SC4 explicit grep returns no matches: `! grep -rn 'MICMAP_DRIVER_BUILD' src/audio/ src/detection/ src/core/ src/common/` exits 0.
    - Headless `cmake -B build-headless -DMICMAP_BUILD_DRIVER=OFF` configure log contains BOTH `OpenVR not found - SteamVR features will use stub implementation` (proves OpenVR is genuinely absent) AND `STATUS: AssertNoOpenVRInCore: clean (visited N targets)` (proves the guard still walks `micmap_core_runtime` cleanly without OpenVR present).
    - Headless `cmake --build build-headless --target mic_test --config Release` exits 0 — **this is the SC1 acceptance probe**.
    - `dumpbin /exports build/driver/micmap/bin/win64/driver_micmap.dll | findstr /R "^[ ]*[0-9]"` returns exactly one line, containing `HmdDriverFactory` (SC3 — manual MSVC Developer Command Prompt step; record output in SUMMARY).
    - Binary sizes recorded in SUMMARY for `mic_test.exe`, `micmap.exe`, `driver_micmap.dll`. Driver DLL size delta vs pre-Phase-5 baseline ≤ ~5 KB (Pitfall 5-D early warning; record actual delta).
    - `external/openvr` directory restored (if it was renamed during Step 2): verify with `[ -d external/openvr ] || [ ! -d external/openvr.bak ]` exits 0.
  </acceptance_criteria>
  <done>Both build matrices green; full ctest suite green including the two new lints; SC4 grep clean; SC3 dumpbin shows only HmdDriverFactory; binary sizes recorded; external/openvr restored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — SC5 byte-for-byte v1.5 driver behavior on Bigscreen Beyond</name>
  <files>(no source changes — manual verification)</files>
  <read_first>
    - .planning/phases/05-shared-library-extraction/05-VALIDATION.md (Manual-Only Verifications table — SC5 test instructions)
    - .planning/phases/05-shared-library-extraction/05-CONTEXT.md (D-10 — driver TUs do NOT include runtime headers in P5; SC5 byte-identical contract)
    - CLAUDE.md (project instructions — "Visual validation on a real HMD is mandatory" rule; type-checking and build-success do not substitute)
    - .planning/milestones/v1.5-ROADMAP.md (Phase 1-4 UAT scenarios — the baseline behavior to compare against)
  </read_first>
  <what-built>
    Phase 5 has shipped:
    1. The new `micmap_core_runtime` INTERFACE library aggregating four sub-libs (`micmap_audio`, `micmap_detection`, `micmap_core`, `micmap_common`) — `micmap_steamvr` is deliberately excluded (D-05).
    2. The configure-time guard `cmake/AssertNoOpenVRInCore.cmake` that FATAL_ERRORs on any OpenVR leak into the runtime (LIB-03).
    3. Two CTest source-grep lints (`lint_no_openvr_in_core`, `lint_no_driver_macro`) (D-02, D-12, SC4).
    4. Driver, client, and `mic_test.exe` all link the new runtime; `mic_test.exe` builds without OpenVR present (SC1).
    5. The driver DLL exports only `HmdDriverFactory` (SC3, unchanged).
    6. **No driver translation unit was modified.** `driver_micmap.dll` only gained a PRIVATE link line in `driver/CMakeLists.txt` — no source code changed (D-10 link-only contract).
    
    The byte-for-byte invariant of SC5 derives from item 6: because the driver source is unchanged and the new link is to header-source-free INTERFACE-aggregated STATIC libs that the driver does not include any header from, the driver's compiled object code is bit-identical to v1.5 modulo a handful of additional symbols that the linker may resolve from the new statically-linked archives only if referenced (and no driver TU references them, so the linker drops them — this is the exact mechanism that makes the byte-identical claim falsifiable on the wire).
  </what-built>
  <how-to-verify>
    Perform this on the **Bigscreen Beyond + Win11 Pro rig**. Cannot be substituted by Win11 VM, build-success, or type-checking.
    
    **Setup (one-time):**
    1. Ensure the v1.5 baseline behavior is fresh in mind — if uncertain, briefly review `.planning/milestones/v1.5-ROADMAP.md` Phase 1-4 UAT exit criteria.
    2. Take a baseline note of any non-default SteamVR settings on the test rig (resolution, refresh rate, dashboard binding, default playback device).
    
    **Install the Phase-5 build:**
    1. From a Visual Studio Developer Command Prompt, build the driver-on matrix in Release: `cmake --build build --config Release`.
    2. Copy the freshly built `build/driver/micmap/bin/win64/driver_micmap.dll` over the currently installed driver under SteamVR's drivers directory (typically `%PROGRAMFILES(X86)%/Steam/steamapps/common/SteamVR/drivers/micmap/bin/win64/`). If unsure of the path, run `vrpathreg show` from the SteamVR `bin/win64/` directory.
    3. Copy the matching `build/bin/Release/micmap.exe` over the currently installed client (the v1.5 installer location, typically `%PROGRAMFILES%/MicMap/bin/`).
    
    **Run the 5-cycle UAT:**
    For each cycle (1 through 5):
    1. Power on the Bigscreen Beyond.
    2. Launch SteamVR. Wait for the headset icon to go solid green in the SteamVR status window.
    3. Launch `micmap.exe` (or wait for SteamVR's app.vrmanifest auto-launch from Phase 3 — whichever matches the v1.5 baseline you are comparing against).
    4. With the headset on, look around the play space. **Confirm: NO laser beam from any phantom controller appears.** (This is the v1.5 SVR-04 / SC5 anti-regression — covered the entire v1.5 sidecar redesign.)
    5. Cover the configured microphone with a finger or hand. **Confirm: SteamVR dashboard toggles ON within ~150ms.**
    6. Uncover the mic. **Confirm: dashboard remains in current state until next cover-mic event.**
    7. Cover the mic a second time. **Confirm: dashboard toggles back OFF.**
    8. Quit SteamVR cleanly via the dashboard "Exit SteamVR" or the system tray icon.
    9. Wait until the headset icon disappears from the status window.
    10. Repeat for the next cycle.
    
    **Pass criteria (all five MUST hold across all 5 cycles):**
    - No laser beam, no phantom controller, no focus loss to a non-existent device.
    - Cover-mic to dashboard-toggle latency feels indistinguishable from v1.5 (subjective — flag any noticeable regression).
    - SteamVR start and stop are clean — no `vrserver.exe` crash, no zombie process in Task Manager after exit.
    - `vrserver.txt` log (under `%LOCALAPPDATA%/openvr/logs/`) contains the same MicMap init line shape as the v1.5 baseline (`MicMap driver v0.1.0 ...` or whatever the embedded version emits).
    - No new driver-side error messages compared to a v1.5 log.
    
    **If ANY cycle fails:** stop, capture `vrserver.txt`, copy the failing build artifacts aside, and either revert to v1.5 (re-install the v1.5 build artifacts) or open a follow-up gap-closure plan documenting the regression. SC5 failure means Phase 5 is not done.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 cycles passed with no behavioral regression vs v1.5. Type "regression: <description>" if any cycle showed behavioral drift. Type "build issue: <description>" if the build artifacts could not be installed.</resume-signal>
  <action>Checkpoint task — execute the manual UAT described in the &lt;how-to-verify&gt; block above on the Bigscreen Beyond + Win11 Pro rig. Install the freshly built driver_micmap.dll and micmap.exe over the v1.5-installed artifacts; perform 5 cycles of SteamVR start, micmap.exe launch, no-laser-beam check, cover-mic-to-dashboard-toggle check, uncover, second-cover, SteamVR clean stop, headset-icon-disappear wait. Record observations on each cycle and flag any behavioral drift vs the v1.5 baseline. This task pauses execution until the user types "approved" or describes the regression.</action>
  <verify>
    <automated>MISSING — manual visual VR validation per CLAUDE.md "visual validation on a real HMD is mandatory" rule. Cannot be automated. Build success and type-checking are insufficient. The Plan 05-01 lints + Plan 05-02 dumpbin check + Plan 05-03 Task 2 build matrix exhaust the automatable surface; SC5 byte-identical-behavior is necessarily a human-eyes check on the wire.</automated>
  </verify>
  <done>All 5 cycles complete; no laser beam in any cycle; cover-mic to dashboard-toggle latency indistinguishable from v1.5 baseline; SteamVR start/stop clean across all cycles; vrserver.txt MicMap init line shape matches v1.5; user has typed "approved" at the resume-signal prompt. Phase 5 is DONE when this checkpoint signs off green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Plan 05-03 modifies build-system metadata in two app CMakeLists.txt files and runs verification. No new IPC, no new I/O, no new runtime code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | (build-time control) | apps/micmap/CMakeLists.txt direct micmap_steamvr link | (safety control, not security) | D-09 keeps `micmap_steamvr` as a direct app-level link rather than aggregating it into `micmap_core_runtime`. The structural choice prevents OpenVR from leaking through the shared runtime to mic_test or any future headless consumer. The mitigation IS the link topology; AssertNoOpenVRInCore.cmake (Plan 05-01) is the post-condition check. |
| T-05-03-02 | (build-time control) | apps/mic_test/CMakeLists.txt single-link reduction | (safety control, not security) | D-08 reduces mic_test's link surface to a single INTERFACE-aggregated reference, making future runtime additions automatically reach the headless harness without consumer edits. This is a maintainability mitigation that also tightens the SC1 invariant: any future runtime expansion that breaks `mic_test` headless build is caught at the next CI run rather than a months-later headless-build attempt. |

**No new attack surface; build-time configure-time guards only.** Plan 05-03 introduces no new runtime code paths, no new network listeners, no new file I/O, no new third-party dependencies. The modifications are pure CMake link-list edits in two app build files plus a manual UAT signoff.
</threat_model>

<verification>
After all three tasks complete:

1. `apps/mic_test/CMakeLists.txt` links exactly `micmap::core_runtime` PRIVATE (D-08).
2. `apps/micmap/CMakeLists.txt` links `micmap::core_runtime` + `micmap_steamvr` + `imgui` + `micmap::bindings` PRIVATE (D-09).
3. `apps/hmd_button_test/CMakeLists.txt` is byte-identical to the pre-plan baseline (`git diff --quiet`).
4. No reference to `micmap_lib` or `micmap::lib` survives anywhere in the source tree (D-07 invariant).
5. Driver-on build matrix is green: `cmake -B build && cmake --build build --config Release && ctest --test-dir build -C Release --output-on-failure` succeeds; ctest report includes both new lints in the green list.
6. Headless build matrix is green: `cmake -B build-headless -DMICMAP_BUILD_DRIVER=OFF && cmake --build build-headless --target mic_test --config Release` succeeds with OpenVR SDK absent (SC1 acceptance probe).
7. SC4 explicit grep returns no matches in the four shared-lib roots.
8. SC3 dumpbin shows exactly `HmdDriverFactory` exported by `driver_micmap.dll`.
9. Manual UAT: 5 cycles of SteamVR start/stop on Bigscreen Beyond rig with v1.5 trigger path passing identically to the v1.5 baseline (SC5).

Phase 5 success criteria mapping after this plan:
- SC1 (headless mic_test build) — covered by Task 2 Step 2.
- SC2 (CI grep / configure-time guard) — covered by Plan 05-01 Tasks 1-4 + Task 2 ctest run here.
- SC3 (dumpbin one export) — covered by Task 2 Step 4.
- SC4 (zero MICMAP_DRIVER_BUILD hits) — covered by Plan 05-01 Task 2/3 + Task 2 Step 3 here.
- SC5 (byte-identical v1.5 behavior) — covered by Task 3 manual UAT here.

Phase 5 is DONE when this plan's checkpoint signs off.
</verification>

<success_criteria>
- [ ] apps/mic_test/CMakeLists.txt: single PRIVATE micmap::core_runtime link (D-08)
- [ ] apps/micmap/CMakeLists.txt: PRIVATE micmap::core_runtime + micmap_steamvr + imgui + micmap::bindings link (D-09)
- [ ] apps/hmd_button_test/CMakeLists.txt: unchanged
- [ ] No micmap_lib / micmap::lib references in apps/, driver/, tests/, src/
- [ ] Driver-on build matrix green (configure + build + ctest)
- [ ] Headless build matrix green: mic_test builds with -DMICMAP_BUILD_DRIVER=OFF and OpenVR absent (SC1)
- [ ] SC4 grep clean: zero MICMAP_DRIVER_BUILD hits in src/{audio,detection,core,common}/
- [ ] SC3 dumpbin: driver_micmap.dll exports only HmdDriverFactory
- [ ] SC5 manual UAT signed off: 5 cycles on Bigscreen Beyond rig, no behavioral regression vs v1.5
</success_criteria>

<manual_uat>
## SC5 — v1.5 Behavioral Regression UAT (mandatory, manual-only)

**Why manual:** Per CLAUDE.md, "visual validation on a real HMD is mandatory — type-checking and build-success do not substitute." SC5 demands byte-for-byte v1.5 driver behavior; the only falsification of "byte-for-byte" that matters is a real headset showing real behavior on the real Bigscreen Beyond + Win11 Pro rig.

**Scope:** 5-cycle SteamVR start/stop with v1.5 trigger path (`POST /button` cover-mic → dashboard toggle).

**Pass criteria:**
- No laser beam / phantom controller in any cycle.
- Cover-mic to dashboard-toggle latency indistinguishable from v1.5 baseline.
- Clean SteamVR start and stop in all 5 cycles (no vrserver.exe crash, no zombie process).
- `vrserver.txt` MicMap init line shape matches v1.5 baseline.

**Fail handling:** Capture `vrserver.txt`, revert to v1.5 install, open gap-closure plan documenting the regression. SC5 failure means Phase 5 is not done.

This UAT is gated behind Task 3 (`checkpoint:human-verify`); execute-plan will pause for the user signoff before declaring Phase 5 complete.
</manual_uat>

<output>
After completion, create `.planning/phases/05-shared-library-extraction/05-03-SUMMARY.md` documenting:
- Diff summary of `apps/mic_test/CMakeLists.txt` and `apps/micmap/CMakeLists.txt`
- Confirmation of `git diff --quiet -- apps/hmd_button_test/CMakeLists.txt`
- Driver-on build matrix evidence (configure log STATUS line, build success, ctest output)
- Headless build matrix evidence (configure log STATUS lines for both "OpenVR not found" and "AssertNoOpenVRInCore: clean", `mic_test` build success)
- SC4 grep result (`MICMAP_DRIVER_BUILD` zero-hit confirmation)
- SC3 dumpbin output (full export table — must show only HmdDriverFactory)
- Binary size table: pre-Phase-5 vs post-Phase-5 for `mic_test.exe`, `micmap.exe`, `driver_micmap.dll`
- SC5 manual UAT signoff: 5-cycle results + tester signature/timestamp
- Phase 5 success criteria checklist (all 5 ticked)
</output>
