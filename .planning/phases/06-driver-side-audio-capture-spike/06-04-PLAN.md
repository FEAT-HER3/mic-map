---
phase: 06-driver-side-audio-capture-spike
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md
  - driver/resources/settings/default.vrsettings
autonomous: false
requirements: [MIG-01]
must_haves:
  truths:
    - "D-17(1) Flag-ON real-hardware capture is observed: vrserver.txt shows worker constructed, MTA CoInitializeEx outcome, WASAPI device opened, ~80-120 RMS lines covering ~1 s, capture stays alive ≥30 s. SC1 evidence committed to 06-UAT.md."
    - "D-17(2) HMD wake/sleep × 2 cycles complete with audio worker alive: no leaked handles in Process Explorer, no crash. Observation committed to 06-UAT.md."
    - "D-17(3) Single SteamVR-restart-without-quit cycle completes without AUDCLNT_E_DEVICE_IN_USE on second Init; IMMNotificationClient register/unregister observable in vrserver.txt; no leaked WASAPI handle on vrserver.exe PID. SC2/SC5 lifecycle evidence committed to 06-UAT.md."
    - "D-17(4) Flag-OFF regression: with enable_driver_audio = false, hmd_button_test.exe drives the v1.5 trigger path (POST /button → CommandQueue → /input/system/click) and the SteamVR dashboard toggles. Driver behavior byte-identical to Phase 5 baseline. SC4 evidence committed to 06-UAT.md."
    - "RMS log budget bounded ≈1 s confirmed by `grep -c 'MicMap audio: rms\\['` count in vrserver.txt being in the 80-120 range — well below the 1000+ that would indicate D-08 budget violation."
    - "Default vrsettings file restored to `enable_driver_audio: false` at end of UAT so the shipped default is OFF (D-19, D-20 — flag stays OFF on main until Phase 7 closes)."
  artifacts:
    - path: ".planning/phases/06-driver-side-audio-capture-spike/06-UAT.md"
      provides: "Phase 6 UAT log: D-17 (1)–(4) observations + vrserver.txt excerpts + Process Explorer observations + sign-off block per D-18"
      contains: "D-17"
    - path: "driver/resources/settings/default.vrsettings"
      provides: "Final shipped state — enable_driver_audio: false (was toggled to true during D-17(1)-(3); restored at end of UAT)"
      contains: "enable_driver_audio"
  key_links:
    - from: "06-UAT.md"
      to: "vrserver.txt evidence"
      via: "pasted log excerpt covering the flag-ON 1 s RMS window per D-18"
      pattern: "MicMap audio: rms\\["
    - from: "06-UAT.md"
      to: "06-CONTEXT.md D-17"
      via: "each of (1), (2), (3), (4) sub-step has a check-marked entry with observed outcome"
      pattern: "D-17"
---

<objective>
Wave 3 — manual real-hardware UAT on the Bigscreen Beyond + Win11 Pro rig per CONTEXT D-17. This is the spike's go/no-go gate: Phase 6 only ships if WASAPI capture works inside `vrserver.exe`'s DLL host on real hardware. Plans 01-03 produced a build that is automated-test green; this plan exercises it on real hardware across four scenarios — flag-ON capture (D-17(1) → SC1), HMD wake/sleep cycles (D-17(2)), single SteamVR-restart-without-quit cycle (D-17(3) → SC2/SC5), and flag-OFF regression via `hmd_button_test.exe` (D-17(4) → SC4). The output is `06-UAT.md` with vrserver.txt log excerpts and operator observations per D-18.

Purpose: prove the highest-risk unknown of the v1.6 milestone — WASAPI inside the vrserver DLL host — actually works on the production hardware target. If any of D-17 (1)-(4) fails, Phase 6 is blocked and must escalate before Phase 7 begins (per STATE.md "Blockers/Concerns").

Output: `.planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` with all four D-17 sub-steps' observations + vrserver.txt evidence; restored `default.vrsettings` flag back to false.

This plan is `autonomous: false` — the executor stops at each manual checkpoint and waits for the operator to drive the hardware and report observations.
</objective>

<execution_context>
@C:/Users/decid/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@C:/Users/decid/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-driver-side-audio-capture-spike/06-CONTEXT.md
@.planning/phases/06-driver-side-audio-capture-spike/06-VALIDATION.md
@.planning/phases/06-driver-side-audio-capture-spike/06-01-PLAN.md
@.planning/phases/06-driver-side-audio-capture-spike/06-02-PLAN.md
@.planning/phases/06-driver-side-audio-capture-spike/06-03-PLAN.md
@./CLAUDE.md

<interfaces>
<!-- The 06-UAT.md template the executor scaffolds for the operator. -->
<!-- Filled in incrementally as each D-17 sub-step completes. -->

```markdown
# Phase 6 — Real-Hardware UAT Log (D-17)

**Tested:** YYYY-MM-DD
**Rig:** Bigscreen Beyond + Win11 Pro (build XX.X.XXXXX)
**Driver build SHA:** <git rev-parse --short HEAD after Plans 01-03>
**Operator:** <name>

## D-17(1) — Flag-ON capture run (SC1)

- [ ] `enable_driver_audio = true` set in `default.vrsettings`
- [ ] SteamVR booted, HMD connected, vrserver.exe running
- [ ] vrserver.txt shows `MicMap: enable_driver_audio = true`
- [ ] vrserver.txt shows `MicMap: audio worker thread COM apartment = MTA (hr=0xXXXXXXXX)` (S_OK or S_FALSE both pass; RPC_E_CHANGED_MODE bails out)
- [ ] vrserver.txt shows ≥80 lines matching `MicMap audio: rms[N]=...` covering the first ~1 s
- [ ] vrserver.txt shows the audio worker survives ≥30 s (no early-exit log lines)
- [ ] `grep -c "MicMap audio: rms\\[" vrserver.txt` is in the 80-120 range (D-08 budget bounded)

### Evidence — vrserver.txt excerpt (~30 lines covering Init + first RMS window):
```
<paste here>
```

**Outcome:** ☐ PASS · ☐ FAIL — `<observations>`

***

## D-17(2) — HMD wake/sleep × 2 cycles

(Run with the D-17(1) session still active.)

- [ ] Sleep HMD; wake HMD (cycle 1)
- [ ] Sleep HMD; wake HMD (cycle 2)
- [ ] vrserver.txt shows no `MicMap: audio worker thread exiting` entries during either cycle
- [ ] Process Explorer: vrserver.exe handle count stable across both cycles (no growth)
- [ ] No crash dialog; no `Sentinel`-style error log

**Outcome:** ☐ PASS · ☐ FAIL — `<observations>`

***

## D-17(3) — SteamVR-restart-without-quit single cycle (SC2/SC5)

(Run with the D-17(1) session still active. Use SteamVR UI's "Restart SteamVR" — must NOT quit vrserver.exe outright.)

- [ ] vrserver.txt shows: AudioWorker.reset() runs first → `MicMap: audio worker thread exiting cleanly` → `MicMap driver cleanup complete`
- [ ] vrserver.txt then shows the second `Init` cycle: `MicMap driver initializing (sidecar mode)` → `MicMap: enable_driver_audio = true` → `MicMap: audio worker thread COM apartment = MTA ...`
- [ ] No `AUDCLNT_E_DEVICE_IN_USE` (0x88890004) anywhere in vrserver.txt during the second Init
- [ ] IMMNotificationClient lifecycle: register on first Init → unregister on first Cleanup → re-register on second Init (observable as device-add/-remove log lines, or absence of stale-handle errors)
- [ ] Process Explorer: no leaked WASAPI handle on vrserver.exe PID after the second Cleanup completes (text observation acceptable per D-18)

### Evidence — vrserver.txt excerpt covering Cleanup → Init transition:
```
<paste here>
```

**Outcome:** ☐ PASS · ☐ FAIL — `<observations>`

***

## D-17(4) — Flag-OFF regression (SC4)

- [ ] `enable_driver_audio = false` set in `default.vrsettings` (restored from D-17(1) test value)
- [ ] SteamVR rebooted from cold
- [ ] vrserver.txt startup banner: `MicMap: enable_driver_audio = false` present, NO `MicMap: audio worker thread` lines whatsoever (D-03 — no AudioWorker constructed)
- [ ] `hmd_button_test.exe` issues POST /button → CommandQueue → `/input/system/click`; SteamVR dashboard toggles
- [ ] vrserver.txt diff against Phase 5 baseline: only the new flag-read log line is added; everything else byte-identical (or as near as drift permits — note any unexpected delta)

**Outcome:** ☐ PASS · ☐ FAIL — `<observations>`

***

## Final state

- [ ] `default.vrsettings` restored to `enable_driver_audio: false` (shipped default per D-19/D-20)
- [ ] All four D-17 sub-steps PASS

**Phase 6 UAT sign-off:** ☐ APPROVED · ☐ BLOCKED — `<operator name>`, `<date>`
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Scaffold 06-UAT.md and operator-prep the rig</name>
  <files>
    .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md,
    driver/resources/settings/default.vrsettings
  </files>
  <read_first>
    - .planning/phases/06-driver-side-audio-capture-spike/06-CONTEXT.md (D-17 sub-steps + D-18 evidence requirements)
    - .planning/phases/06-driver-side-audio-capture-spike/06-VALIDATION.md (§"Manual-Only Verifications" — what each SC observation must contain)
  </read_first>
  <action>
The executor (1) creates `06-UAT.md` from the template in the interfaces block above, populating the date, driver SHA (`git rev-parse --short HEAD`), and rig fields. (2) Toggles `enable_driver_audio` to `true` in `driver/resources/settings/default.vrsettings` for the D-17(1)-(3) test runs (it MUST be restored to `false` in Task 5).

(3) Provides the operator with the exact CLI invocations needed:
- `cmake --build build --config Release` (full driver-on build with OpenVR present)
- Path to the driver DLL output: `build/driver/micmap/bin/win64/driver_micmap.dll`
- Path to vrserver.txt: `%TEMP%\StreamVR\logs\vrserver.txt` (or wherever the local SteamVR logs land — confirm location with operator)
- Path to `hmd_button_test.exe` for D-17(4)
  </action>
  <verify>
1. Open `.planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` — confirm template scaffolded with all four D-17 sub-steps as empty checkboxes.
2. Open `driver/resources/settings/default.vrsettings` — confirm `enable_driver_audio` is now `true`.
3. Confirm a fresh driver build is staged: `ls build/driver/micmap/bin/win64/driver_micmap.dll` shows a recent timestamp.
4. Confirm rig is ready: Bigscreen Beyond connected, Win11 Pro session active, SteamVR not running yet.
5. Reply with one of:
   - `READY` — proceed to D-17(1)
   - `BLOCKED: <reason>` — escalate before continuing (e.g., rig unavailable, build failure surfaced last-minute)
  </verify>
  <resume-signal>Reply `READY` to proceed to Task 2 (D-17 (1)+(2)+(3) consolidated session), or `BLOCKED: <reason>` to halt the phase.</resume-signal>
  <done>
    - File `.planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` exists.
    - `grep -c "D-17" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` >= 4 (all four sub-steps scaffolded).
    - `grep -c "Evidence" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` >= 2 (D-17(1) and D-17(3) evidence blocks).
    - `python -c "import json; d=json.load(open('driver/resources/settings/default.vrsettings')); assert d['driver_micmap']['enable_driver_audio']==True"` exits 0 (flag toggled to true for the test run).
    - Operator has confirmed `READY` (resume signal received).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs D-17(1) flag-ON capture + D-17(2) HMD wake/sleep + D-17(3) SteamVR restart cycle</name>
  <files>
    .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md
  </files>
  <read_first>
    - .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md (the scaffold from Task 1)
    - .planning/phases/06-driver-side-audio-capture-spike/06-CONTEXT.md (D-17(1)/(2)/(3) procedures verbatim)
    - .planning/phases/06-driver-side-audio-capture-spike/06-VALIDATION.md (§"Manual-Only Verifications" — SC1, SC2, SC5 evidence requirements)
  </read_first>
  <action>
This is the live real-hardware run. The operator drives a SINGLE continuous SteamVR session covering D-17(1) through D-17(3):

**D-17(1) — Flag-ON capture run (~5 minutes):**
1. With `enable_driver_audio = true` in `default.vrsettings` (Task 1 toggled), boot SteamVR cold.
2. Wait for first-frame Init log + audio worker construction logs in vrserver.txt.
3. Let the session run ≥30 seconds. Operator may speak / make noise into the mic.
4. Capture: vrserver.txt excerpt covering driver Init through ~1 s of RMS lines (~30 lines of context).
5. Run `grep -c "MicMap audio: rms\\[" <vrserver.txt>` and report the count (should be 80-120 per D-08).

**D-17(2) — HMD wake/sleep × 2 (~2 minutes, with the D-17(1) session still running):**
6. Sleep HMD (set down on table or use SteamVR sleep button); wait ~10 s; wake HMD. Repeat once.
7. Capture: any vrserver.txt log lines added during the cycles (typically very few — the audio worker is independent of HMD activation per D-CONTEXT "audio worker is independent of HMD-handle state").
8. Process Explorer: open vrserver.exe handles view; verify count stable.

**D-17(3) — SteamVR-restart-without-quit single cycle (~3 minutes, with the D-17(1) session still running):**
9. From SteamVR Settings → Developer → "Restart SteamVR" (or status menu equivalent — IMPORTANT: must NOT exit/quit vrserver.exe; the in-process Cleanup→Init transition is what we're testing).
10. Wait for the second Init cycle to complete (HMD reconnects, dashboard works).
11. Capture: vrserver.txt excerpt covering the Cleanup → second Init transition (~30 lines).
12. Confirm `AUDCLNT_E_DEVICE_IN_USE` (0x88890004) does not appear anywhere during the second Init.
13. Process Explorer: verify no leaked WASAPI handle on vrserver.exe PID after Cleanup completes.

The operator updates `06-UAT.md` checkboxes + observations + evidence excerpts as each sub-step completes. The executor reads back the file at the end of this task to verify completeness.
  </action>
  <verify>
After D-17(1)+(2)+(3) complete, reply with one of:

- `D17-1-2-3 PASS` — all three sub-steps' checkboxes are ticked in `06-UAT.md` with operator observations + vrserver.txt evidence pasted into each Evidence block. The executor verifies completeness by reading `06-UAT.md`.
- `D17-1-2-3 FAIL: <which sub-step + symptom>` — escalate. Phase 6 cannot ship if any of (1)-(3) fails. The most critical signal is D-17(3) AUDCLNT_E_DEVICE_IN_USE on second Init — that would indicate Pitfall 4 is not fully mitigated and the AudioWorker teardown order needs review (likely a Plan 02 or Plan 03 fix-forward, not a Phase 6 blocker, but the spike outcome would be "feasible with caveats" and the operator should escalate).
- `D17-1-2-3 PARTIAL: <observations>` — e.g., D-17(1) PASS but D-17(2) had an unexpected log line; capture the partial result and route to the planner for triage before continuing to D-17(4).
  </verify>
  <resume-signal>Reply `D17-1-2-3 PASS` to proceed to D-17(4) (Task 3), or `D17-1-2-3 FAIL: ...` / `D17-1-2-3 PARTIAL: ...` to halt for triage.</resume-signal>
  <done>
    - `grep -c "\\[x\\]" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` >= 15 (rough count: ≥7 D-17(1) checkboxes + ≥4 D-17(2) + ≥4 D-17(3) ticked).
    - `06-UAT.md` D-17(1) Evidence block contains a non-empty fenced code block (vrserver.txt excerpt pasted, not the literal `<paste here>` placeholder).
    - `06-UAT.md` D-17(3) Evidence block contains a non-empty fenced code block (vrserver.txt excerpt pasted).
    - `grep -c "MicMap audio: rms\\[" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` >= 5 (the pasted evidence contains real RMS log lines).
    - `06-UAT.md` D-17(1) "Outcome:" line shows PASS (or operator-explicit FAIL if signaled).
    - `06-UAT.md` D-17(2) "Outcome:" line shows PASS.
    - `06-UAT.md` D-17(3) "Outcome:" line shows PASS.
    - Operator has confirmed `D17-1-2-3 PASS` (resume signal received).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runs D-17(4) flag-OFF regression via hmd_button_test.exe</name>
  <files>
    .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md,
    driver/resources/settings/default.vrsettings
  </files>
  <read_first>
    - .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md (the in-progress UAT log)
    - .planning/phases/06-driver-side-audio-capture-spike/06-CONTEXT.md (D-17(4) procedure + SC4 byte-identical contract)
  </read_first>
  <action>
**D-17(4) — Flag-OFF regression (~5 minutes):**

1. The executor toggles `enable_driver_audio` back to `false` in `driver/resources/settings/default.vrsettings`.
2. Operator quits SteamVR completely, then re-boots cold.
3. Operator captures vrserver.txt startup banner: confirm `MicMap: enable_driver_audio = false` present, AND no `MicMap: audio worker` lines whatsoever (D-03 — no AudioWorker constructed).
4. Operator runs `hmd_button_test.exe` (the v1.5 trigger-path harness — see CLAUDE.md "Testing"). The test issues `POST /button` to the driver's HTTP server; the driver enqueues a TapCommand on the CommandQueue; RunFrame drains it and writes to `/input/system/click`; SteamVR's bindings translate that to a system-button press; the SteamVR dashboard toggles.
5. Operator confirms the dashboard appears/disappears on each test invocation. SC4 satisfied — driver behavior byte-identical to Phase 5 v1.5 baseline.
6. Operator updates `06-UAT.md` D-17(4) checkboxes + observations.
  </action>
  <verify>
After D-17(4) completes, reply with one of:

- `D17-4 PASS` — flag-OFF behavior is byte-identical to Phase 5; `hmd_button_test.exe` toggles the dashboard via POST /button. `06-UAT.md` D-17(4) checkboxes ticked + observations recorded.
- `D17-4 FAIL: <symptom>` — flag-OFF behavior differs from Phase 5 baseline. SC4 not satisfied. Escalate. Most likely cause: `default.vrsettings` did not get restored to `false`, OR a stray AudioWorker construction log appears (which would mean Plan 03's `if (driverAudioEnabled_)` gate is broken).
  </verify>
  <resume-signal>Reply `D17-4 PASS` to proceed to UAT sign-off (Task 4), or `D17-4 FAIL: ...` to halt for triage.</resume-signal>
  <done>
    - `python -c "import json; d=json.load(open('driver/resources/settings/default.vrsettings')); assert d['driver_micmap']['enable_driver_audio']==False"` exits 0 (flag restored to false).
    - `06-UAT.md` D-17(4) "Outcome:" line shows PASS.
    - `grep -c "\\[x\\]" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` >= 19 (Task 2 floor + ≥4 D-17(4) checkboxes).
    - Operator has confirmed `D17-4 PASS`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Finalize 06-UAT.md sign-off block + commit</name>
  <files>
    .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md,
    driver/resources/settings/default.vrsettings
  </files>
  <read_first>
    - .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md (verify all four D-17 sub-steps PASS)
    - driver/resources/settings/default.vrsettings (verify enable_driver_audio == false — the shipped default per D-19/D-20)
  </read_first>
  <action>
**Step 1 — finalize sign-off.** Edit the "Final state" section of `06-UAT.md`:
- Tick the `default.vrsettings` restored-to-false checkbox (Task 3 already toggled).
- Tick the "All four D-17 sub-steps PASS" checkbox.
- Fill in the sign-off line: `**Phase 6 UAT sign-off:** ☑ APPROVED · ☐ BLOCKED — <operator name from Task 1>, YYYY-MM-DD`.

**Step 2 — final state assertions.**
- Re-confirm `default.vrsettings` parses as JSON with `enable_driver_audio == false`.
- Re-run `ctest --test-dir build-headless --output-on-failure` to confirm the toggling did not regress anything (it shouldn't — vrsettings is not part of any ctest, but the principle of "leave the tree green at end of UAT" is the rule).

**Step 3 — commit.** Use the GSD commit helper:
```
gsd-sdk query commit "docs(06): UAT sign-off — D-17(1)-(4) all PASS on Bigscreen Beyond + Win11 Pro" \
  .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md \
  driver/resources/settings/default.vrsettings
```

(The default.vrsettings change committed here is the Task 1 toggle plus the Task 3 restore — net zero behavioral change vs the Plan 02 Task 1 baseline. The diff shows the file as `enable_driver_audio: false` again, identical to its post-Plan-02 state.)
  </action>
  <verify>
    <automated>ctest --test-dir C:/Users/decid/Documents/projects/mic-map/build-headless --output-on-failure</automated>
  </verify>
  <done>
    - `grep -c "APPROVED" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` >= 1 (sign-off line present).
    - `grep -c "Phase 6 UAT sign-off" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md` == 1.
    - `python -c "import json; d=json.load(open('driver/resources/settings/default.vrsettings')); assert d['driver_micmap']['enable_driver_audio']==False"` exits 0.
    - `ctest --test-dir build-headless --output-on-failure` exits 0 (green tree at end of phase).
    - `git log -1 --pretty=%s` matches `^docs\\(06\\): UAT sign-off` (commit landed).
    - Worktree clean: `git status --porcelain` returns no output (all UAT artifacts committed).
  </done>
  <done>06-UAT.md has the APPROVED sign-off, default.vrsettings is back at the shipped default (false), the worktree is clean, and Phase 6 is ready for `/gsd-verify-work` and the milestone `/gsd-next` transition to Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → real-hardware rig | Manual driver session on production-shape hardware; the load-bearing trust boundary is the operator's vrserver.txt observation faithfully transcribed into 06-UAT.md |
| flag-toggle (true) → flag-toggle (false) | Test-only state must be reverted; D-19/D-20 require flag default OFF on main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T5 | I (driver-audio enabled by surprise on shipped users) | default.vrsettings post-UAT state | mitigate | Task 4 acceptance includes the Python JSON assert that `enable_driver_audio == false`. Task 3 explicitly toggles back. Commit message documents the round-trip. D-19/D-20 require flag-OFF on main. |
| T-06-04-01 | T (UAT log fabrication / not faithfully reflecting hardware state) | 06-UAT.md operator entries | mitigate | Acceptance requires non-placeholder fenced code blocks (regex match against the literal placeholder string `<paste here>`); requires `MicMap audio: rms[` to appear in the pasted evidence (real-hardware artifact, not synthetic). Operator sign-off line attaches name + date. |
| T-06-04-02 | D (operator stalls phase indefinitely) | manual checkpoints | accept | This is a real-hardware spike; manual gates are intrinsic. The three checkpoints have clear binary resume signals (`READY` / `D17-1-2-3 PASS` / `D17-4 PASS`) so the operator never has to invent a vocabulary. If hardware is unavailable, `BLOCKED:` halts cleanly. |
| T1, T2, T3, T4, T6 (carry from Plan 02/03) | — | AudioWorker behavior on real hardware | observe | This plan is the verification surface for those mitigations. D-17(1) verifies T1 (CoInit on worker only); D-17(3) verifies T2 + T4 (UAF + handle leak via Cleanup→Init); D-17(2)+(3) verify T3 (no shutdown deadlock); UAT log RMS line count verifies T6 budget. |
</threat_model>

<verification>
**Final state checks (Task 4):**

```bash
# Tree green
ctest --test-dir build-headless --output-on-failure

# UAT log complete
grep -c "APPROVED" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md
grep -c "MicMap audio: rms\\[" .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md   # ≥ 5

# Default vrsettings shipped-default restored
python -c "import json; d=json.load(open('driver/resources/settings/default.vrsettings')); assert d['driver_micmap']['enable_driver_audio']==False"

# Worktree clean (everything committed)
git status --porcelain
```
</verification>

<success_criteria>
- All four D-17 sub-steps observed and recorded in `06-UAT.md` with vrserver.txt evidence excerpts (D-17(1) and D-17(3) Evidence blocks contain real log lines, not placeholder text).
- D-17(1) PASS — SC1 satisfied (flag-ON capture works on real hardware; ~80-120 RMS lines logged within budget).
- D-17(2) PASS — HMD wake/sleep × 2 cycles complete without leaked handles or crash.
- D-17(3) PASS — single SteamVR-restart-without-quit cycle completes without `AUDCLNT_E_DEVICE_IN_USE`; SC2/SC5 evidence captured.
- D-17(4) PASS — `hmd_button_test.exe` proves flag-OFF byte-identical to Phase 5 v1.5 baseline (SC4).
- `default.vrsettings` restored to `enable_driver_audio: false` (D-19/D-20 — flag-OFF on main).
- Sign-off line present in `06-UAT.md` with operator name + date.
- Worktree clean; UAT log + vrsettings round-trip committed.
- Phase 6 ready for `/gsd-verify-work` and milestone transition to Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/06-driver-side-audio-capture-spike/06-04-SUMMARY.md`
</output>
