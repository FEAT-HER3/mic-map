---
phase: 09-training-migration
plan: 05
type: execute
wave: 5
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - .planning/phases/09-training-migration/09-UAT.md
autonomous: false
requirements: [TRAIN-01, TRAIN-02, TRAIN-03, TRAIN-04, TRAIN-05, TRAIN-06, TEST-04, IPC-06]
tags: [phase-9, training-migration, wave-5, uat, hardware-loop, manual-checkpoint]

must_haves:
  truths:
    - "Real-hardware UAT regimen D-39(1)..(10) executed on Bigscreen Beyond + Win11 Pro rig with operator sign-off"
    - "All 10 D-39 cases recorded in 09-UAT.md with PASS/FAIL/N/A disposition + driver SHA + rig metadata + operator name + date"
    - "End-to-end training round-trip verified on real hardware: Train → cover mic → finalize → driver writes training_data.bin → client closes/reopens → file loads on driver Init → cover mic again → dashboard toggles (D-39(1))"
    - "Cancel mid-session leaves training_data.bin unmodified (mtime+hash compared before/after — D-39(2))"
    - "Recompute → preview → finalize round-trip produces persisted profile reflecting recomputed thresholds (D-39(3))"
    - "Validation rejection envelopes confirmed via curl: extra fields, missing confirm, out-of-range sensitivity all return HTTP 400 with {field, reason}; driver state unchanged on rejection (D-39(4))"
    - "30 s orphan timeout fires correctly: start training, wait 30 s without covering mic → session auto-cancels with last_error=training_timed_out_no_samples; mode flips back to Detecting (D-39(5))"
    - "Driver-down during training UX recovers gracefully: ECONNREFUSED detected within 1 s, Train UI re-enables on driver restart, in-memory session lost, existing training_data.bin untouched (D-39(6))"
    - "AssertNoClientTraining lint go-live verified: grep apps/micmap/ returns zero hits for 4 forbidden tokens; CI fails on regression (D-39(7))"
    - "CI corpus replay passes: mic_test --replay-dir tests/corpus/replay --expect-triggers-from manifest.json exits 0; replay.json schema validates (D-39(8))"
    - "Replay determinism verified: 3 consecutive runs produce byte-identical replay_results.json (D-39(9))"
    - "50-cycle training stress passes: rapid POST /training/start → cancel cycles show no leaked WASAPI handles + no leaked file handles + training_data.bin integrity preserved (D-39(10))"
    - "default.vrsettings remains enable_driver_audio=false + enable_driver_detection=false post-UAT (P10 owns flag flips per D-40)"
  artifacts:
    - path: ".planning/phases/09-training-migration/09-UAT.md"
      provides: "manual UAT sign-off document with 10 case rows, PASS/FAIL disposition, driver SHA, rig metadata, operator name, dates"
      contains: "D-39"
  key_links:
    - from: ".planning/phases/09-training-migration/09-UAT.md"
      to: ".planning/phases/09-training-migration/09-CONTEXT.md"
      via: "10-case UAT regimen mirrors D-39 in CONTEXT verbatim"
      pattern: "D-39"
---

<objective>
Wave 5: Manual UAT on real hardware. Phase 9 cannot ship without a Bigscreen Beyond + Win11 Pro pass on the 10 D-39 cases — the ROADMAP marks the phase NEEDS VALIDATION because the training UX commit/discard pattern was designed from first principles (no v1.5 prior art).

This plan has ONE task: scaffold `09-UAT.md`, then PAUSE at a `checkpoint:human-verify` task while the operator runs the regimen on the rig and records results. Sign-off transitions Phase 9 to "completed" status; flag flips and client-side detection deletion are deferred to Phase 10.

Plan boundaries:
- IN: 09-UAT.md scaffold creation; manual checkpoint for the 10-case regimen.
- OUT: any code changes (the prior 5 plans cover all production code). UAT is pure verification.
- INHERIT: P6/P7/P8 UAT regimen shape — operator+SHA+date metadata, per-case row, PASS/FAIL disposition, post-UAT default-flag-restore confirmation.

Why autonomous: false — this plan contains a real-hardware checkpoint that must pause for human action.
</objective>

<execution_context>
@$HOME/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-training-migration/09-CONTEXT.md
@.planning/phases/09-training-migration/09-VALIDATION.md
@.planning/phases/09-training-migration/09-UI-SPEC.md
@.planning/phases/09-training-migration/09-00-PLAN.md
@.planning/phases/09-training-migration/09-01-PLAN.md
@.planning/phases/09-training-migration/09-02-PLAN.md
@.planning/phases/09-training-migration/09-03-PLAN.md
@.planning/phases/09-training-migration/09-04-PLAN.md
@.planning/phases/06-driver-side-audio-capture-spike/06-UAT.md
@.planning/phases/08-ipc-contract-reshape/08-UAT.md

<interfaces>
<!-- Prior UAT documents to mirror in shape. Executor reads these before scaffolding 09-UAT.md. -->

P6 UAT shape (`.planning/phases/06-driver-side-audio-capture-spike/06-UAT.md`):
- Frontmatter with phase / created / tested / driver_sha / rig metadata / operator placeholders
- Per-case rows in a table or H3 sections: case ID, description, expected outcome, actual outcome, disposition, evidence (logs/screenshots).
- Final approval line.

P8 UAT shape (`.planning/phases/08-ipc-contract-reshape/08-UAT.md` — the most recent shipped UAT — most directly applicable):
- Read this file first to confirm the current convention for operator sign-off.

CONTEXT D-39 — 10-case regimen verbatim:
1. Training round-trip on real hardware
2. Cancel mid-session
3. Recompute → preview → finalize
4. Validation rejection (curl-driven)
5. Orphan timeout (30 s)
6. Driver-down during training UX
7. AssertNoClientTraining lint go-live verification
8. CI corpus replay
9. Replay determinism
10. Stress (50 rapid start→cancel cycles)

CONTEXT D-40 — post-UAT default-flag confirmation (mirror P7 D-27):
- After UAT completes, verify `driver/resources/default.vrsettings` still has `enable_driver_audio=false` and `enable_driver_detection=false` (P10 owns flips). Operator confirms in 09-UAT.md.

Bigscreen Beyond rig metadata (typical fields — operator fills):
- HMD: Bigscreen Beyond
- OS: Windows 11 Pro
- SteamVR version: e.g. 2.5.x
- Driver SHA: from `git rev-parse --short HEAD` at start of UAT
- Operator: name/email
- Test date: ISO 8601
- Driver build flavor: Debug or RelWithDebInfo
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → 09-UAT.md sign-off | Trusted single operator; sign-off is the gate |
| real hardware → driver runtime | Operator interacts with physical mic + HMD; observed behavior is the ground truth |
| HTTP localhost endpoints (curl) → driver | Validates 400/409/503 envelope shapes hands-on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Repudiation | UAT sign-off without rig metadata | mitigate | 09-UAT.md frontmatter REQUIRES driver_sha + rig + operator + date — checker validates these are non-placeholder before phase-complete. |
| T-09-05-02 | Tampering | sign-off forged for cases that didn't run | accept | Solo-developer trust model (CLAUDE.md philosophy). UAT is honor-system; the 10-case detail discourages skipping by making "I just signed yes" obviously wrong. |
| T-09-05-03 | DoS | UAT case 10 stress test damages training_data.bin | mitigate | Pre-UAT: copy `%APPDATA%\MicMap\training_data.bin` to a backup path. Post-stress: hash compare vs backup; restore if integrity lost. Document the backup step in 09-UAT.md case 10. |
| T-09-05-04 | Information disclosure | UAT logs include user voice samples | mitigate | All UAT cases use synthetic mic-cover (cover the mic with a hand) — no voice/conversation captured. Per CONTEXT D-17 RAM-only sample policy, even captured samples are not persisted. |
| T-09-05-05 | EoP | UAT case 6 "kill SteamVR" leaves driver in inconsistent state | mitigate | UAT case 6 explicit checks: after kill+restart, `%APPDATA%\MicMap\training_data.bin` mtime and hash unchanged from pre-kill (driver only writes on finalize; mid-session kill drops in-memory state, file untouched per D-14 / D-23). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold .planning/phases/09-training-migration/09-UAT.md with 10-case template</name>
  <files>.planning/phases/09-training-migration/09-UAT.md</files>
  <read_first>
    - .planning/phases/06-driver-side-audio-capture-spike/06-UAT.md (verbatim shape — frontmatter + case rows + sign-off)
    - .planning/phases/08-ipc-contract-reshape/08-UAT.md (most recent UAT — current convention)
    - .planning/phases/09-training-migration/09-CONTEXT.md decision D-39 (10-case regimen)
    - .planning/phases/09-training-migration/09-CONTEXT.md decision D-40 (post-UAT default flag confirmation)
  </read_first>
  <action>
Create `.planning/phases/09-training-migration/09-UAT.md`. Use Write tool.

Mirror the P6/P8 UAT shape; populate the 10 case rows from CONTEXT D-39; leave operator/date/SHA/result fields as placeholders that get filled when the operator runs the rig.

Document content:

```markdown
---
phase: 09-training-migration
type: uat
created: <YYYY-MM-DD>
tested: <YYYY-MM-DD or "pending">
driver_sha: <git rev-parse --short HEAD or "pending">
rig:
  hmd: Bigscreen Beyond
  os: Windows 11 Pro
  steamvr_version: <pending>
  build_flavor: <Debug|RelWithDebInfo>
operator: <pending>
status: in_progress
---

# Phase 9 — Training Migration UAT

**Regimen** per `.planning/phases/09-training-migration/09-CONTEXT.md` D-39 (10 cases). All 10 must reach a PASS or N/A disposition before phase-complete; FAIL on any case blocks phase sign-off and may require returning to a prior plan.

**Pre-UAT setup**:
- Build driver + client + mic_test from current HEAD (Debug or RelWithDebInfo).
- Install via Inno Setup output (or copy binaries to %ProgramFiles%\MicMap matching v1.5/P8 layout).
- Verify SteamVR sees the driver: `vrpathreg show` lists `driver_micmap`.
- Set `enable_driver_audio=true` AND `enable_driver_detection=true` in `default.vrsettings` for the duration of UAT (per D-40 these MUST be flipped back to false post-UAT — see Sign-Off section).
- Backup existing `%APPDATA%\MicMap\training_data.bin` to `training_data.bin.preuat` (case 10 stress restoration).

**Pre-UAT hash record**:
```
sha256sum %APPDATA%\MicMap\training_data.bin > training_data_pre_uat.sha256
```

---

## Case D-39(1) — Training round-trip on real hardware

**Goal**: End-to-end training session writes new profile; subsequent detection uses it.

**Steps**:
1. Launch SteamVR + ensure driver loaded (`/health` returns 200).
2. Launch micmap.exe; verify Training pane shows "Train Pattern" button enabled.
3. Click "Train Pattern". Verify UI flips to "Cover mic now!" (orange) + progress bar at 0/100.
4. Cover the mic with a hand for ~10 seconds (sample collection at 5–10 Hz client poll).
5. When state == "ready", verify preview block shows sensitivity / energy_threshold / spectral profile summary.
6. Click "Confirm & Save". Verify 3 s "Profile saved" toast (green).
7. Close client. Reopen client.
8. Verify Training pane returns to Idle with "Status: Profile trained and ready" (green).
9. Cover the mic again — verify the SteamVR dashboard toggles (in-process detection trigger).

**Expected**: dashboard toggles successfully on step 9.
**Disposition**: ⬜ pending  (PASS / FAIL / N/A — fill at test time)
**Evidence**: <link to log excerpt or screenshot>
**Operator notes**:

---

## Case D-39(2) — Cancel mid-session

**Goal**: Cancel during collecting state discards samples without modifying training_data.bin.

**Steps**:
1. Pre-record `sha256sum %APPDATA%\MicMap\training_data.bin` (call it H_before).
2. Click "Train Pattern".
3. Cover mic for ~2 s (collect ~10 samples).
4. Click "Cancel Training".
5. Verify UI returns to Idle (no toast — D-13 cancel is its own confirmation).
6. Hash `%APPDATA%\MicMap\training_data.bin` (call it H_after).

**Expected**: H_before == H_after (file unchanged); driver mode == Detecting (verify via `curl http://127.0.0.1:27115/health` showing `driver_training_active=false`).
**Disposition**: ⬜ pending
**Evidence**:
**Operator notes**:

---

## Case D-39(3) — Recompute → preview → finalize

**Goal**: Recompute replaces preview in-place; finalize persists the recomputed thresholds.

**Steps**:
1. Click "Train Pattern" + cover mic to ready state.
2. Note the preview's sensitivity (call it S0) and energy_threshold (E0).
3. From a separate terminal: `curl -X POST -H "Content-Type: application/json" -d "{\"sensitivity\":0.3}" http://127.0.0.1:27115/training/recompute`.
4. Verify HTTP 200 response body contains updated `thresholds_preview` with sensitivity=0.3 and a different energy_threshold (E1 ≠ E0).
5. In the client UI, observe the next /training/progress poll picks up the new preview rows.
6. Click "Confirm & Save".
7. Read `%APPDATA%\MicMap\training_data.bin` and verify the persisted profile reflects the recomputed thresholds (binary diff vs pre-recompute snapshot OR re-train + cover-mic detection sensitivity check).

**Expected**: Persisted profile matches recomputed thresholds (S=0.3, E=E1).
**Disposition**: ⬜ pending
**Evidence**:
**Operator notes**:

---

## Case D-39(4) — Validation rejection (curl-driven)

**Goal**: HTTP 400 envelope with {field, reason}; driver state unchanged on rejection.

**Steps**: Run each curl from a terminal; verify status code + body. Driver should stay in Detecting mode throughout (no session created/destroyed via these calls).

| # | Curl invocation | Expected status | Expected body |
|---|-----------------|-----------------|---------------|
| 4a | `curl -X POST http://127.0.0.1:27115/training/start -H "Content-Type: application/json" -d '{"foo":"bar"}'` | 400 | `{"field":"foo","reason":"unknown field"}` (or "(structural)" depending on validator path) |
| 4b | `curl -X POST http://127.0.0.1:27115/training/finalize -H "Content-Type: application/json" -d '{}'` | 400 | `{"field":"confirm","reason":"missing required field"}` |
| 4c | `curl -X POST http://127.0.0.1:27115/training/recompute -H "Content-Type: application/json" -d '{"sensitivity":2.0}'` | 400 | `{"field":"sensitivity","reason":"must be in [0.0, 1.0]; got 2.000000"}` |
| 4d | `curl -X POST http://127.0.0.1:27115/training/recompute -H "Content-Type: application/json" -d '{"sensitivity":-0.1}'` | 400 | `{"field":"sensitivity","reason":"must be in [0.0, 1.0]; got -0.100000"}` |
| 4e | `curl -X POST http://127.0.0.1:27115/training/start -H "Content-Type: application/json" -d 'not json'` | 400 | `{"field":"(structural)","reason":"malformed JSON body"}` |

After all 5 calls: `curl http://127.0.0.1:27115/health` returns `driver_training_active=false`.

**Disposition**: ⬜ pending
**Evidence**:
**Operator notes**:

---

## Case D-39(5) — Orphan timeout (30 s)

**Goal**: 30 s no-new-accepted-sample timeout transitions Collecting → Cancelled with last_error.

**Steps**:
1. Click "Train Pattern" — driver enters Training mode.
2. Do NOT cover the mic. Wait 35 seconds (10 s margin past the 30 s timeout).
3. Observe client UI: progress bar shows 0/100; eventually transitions back to Idle.
4. Verify `curl http://127.0.0.1:27115/training/progress` (or check the last poll's payload) shows `state=cancelled` with `last_error=training_timed_out_no_samples`.
5. Verify `curl http://127.0.0.1:27115/health` shows `driver_training_active=false`.
6. Optional: verify the last_error string surfaces in the UI as "Training timed out — no samples collected in 30 s" (destructive color) per UI-SPEC §"Cancelled / finalized terminal states".

**Expected**: Auto-cancel within 30-32 s of "Train Pattern" click; driver mode flips back to Detecting.
**Disposition**: ⬜ pending
**Evidence**:
**Operator notes**:

---

## Case D-39(6) — Driver-down during training UX

**Goal**: Mid-session SteamVR kill recovers gracefully; existing training_data.bin untouched.

**Steps**:
1. Pre-record `sha256sum %APPDATA%\MicMap\training_data.bin` (H_before).
2. Click "Train Pattern" + cover mic for 5 s (~50 samples collected, NOT yet finalized).
3. Kill SteamVR (Task Manager → SteamVR.exe / vrserver.exe).
4. Observe client UI: within ~1 s, /health poll fails with ECONNREFUSED; client driver-loaded indicator flips red; Train Pattern UI re-disables.
5. Restart SteamVR; wait for driver to load.
6. Verify client UI driver-loaded flips green within 1-2 health-poll cycles.
7. Hash `%APPDATA%\MicMap\training_data.bin` (H_after).
8. Verify Train Pattern button is enabled again (Idle state).

**Expected**: H_before == H_after (file untouched); UI re-enables Train Pattern after driver restart; in-memory session is lost (which is correct per D-14 — no resume).
**Disposition**: ⬜ pending
**Evidence**:
**Operator notes**:

---

## Case D-39(7) — AssertNoClientTraining lint go-live verification

**Goal**: cmake/AssertNoClientTraining.cmake fires on regression; CI fails on reintroduction.

**Steps**:
1. From repo root: `cd build-uat; ctest -R AssertNoClientTraining --output-on-failure`. Verify PASS (clean — no violations).
2. Manual regression test: temporarily add a line to `apps/micmap/main.cpp` such as `// test: detector->addTrainingSample(nullptr, 0);` (the comment IS a violation because the lint regex matches the bare token, not language semantics).
3. Re-run `ctest -R AssertNoClientTraining --output-on-failure`. Verify FAIL with FATAL message identifying main.cpp.
4. Revert the change.
5. Re-run ctest. Verify PASS again.

**Expected**: lint catches the regression; lint goes back to clean after revert.
**Disposition**: ⬜ pending
**Evidence**: ctest output excerpt for FAIL run.
**Operator notes**:

---

## Case D-39(8) — CI corpus replay

**Goal**: mic_test --replay-dir against seed corpus exits 0 with all 3 entries passing.

**Steps**:
1. Build mic_test: `cmake --build build-uat --target mic_test`.
2. Run: `mic_test --replay-dir tests/corpus/replay --expect-triggers-from tests/corpus/replay/manifest.json --json-output replay_results.json`.
3. Verify exit code 0.
4. Verify `replay_results.json` validates against the D-30 schema (config_path / profile_path / files[] / summary{} keys present; each file entry has wav / duration_s / sample_rate / channels / expected_triggers / observed_triggers / tolerance / pass / triggers).
5. Verify `summary.passed == 3 && summary.failed == 0`.
6. Verify each file entry has `pass: true`.

**Expected**: all 3 corpus entries pass.
**Disposition**: ⬜ pending
**Evidence**: replay_results.json contents (or hash).
**Operator notes**:

---

## Case D-39(9) — Replay determinism

**Goal**: Same WAV + same profile + same config = byte-identical replay output.

**Steps**:
1. Run case D-39(8) command 3 times back-to-back, each writing to a different output:
    - `... --json-output replay_run_1.json`
    - `... --json-output replay_run_2.json`
    - `... --json-output replay_run_3.json`
2. Compare files: `cmp replay_run_1.json replay_run_2.json && cmp replay_run_2.json replay_run_3.json`.
3. Verify all three are byte-identical.

**Expected**: All three output files byte-identical (cmp returns 0 / no output).
**Disposition**: ⬜ pending
**Evidence**: cmp exit codes for all comparisons.
**Operator notes**:

---

## Case D-39(10) — Stress (50 rapid start→cancel cycles)

**Goal**: No leaked WASAPI handles / file handles after 50 rapid training start/cancel cycles; training_data.bin integrity preserved.

**Steps**:
1. Pre-record handle counts via Process Explorer (filter by `vrserver.exe`):
   - Total handle count (call it HC0)
   - Audio-related handle count (look for `\Device\KSENUM` or COM apartment markers; alternatively just total)
2. Pre-record `sha256sum %APPDATA%\MicMap\training_data.bin` (H_before).
3. Run a small bash/PowerShell loop:
    ```bash
    for i in {1..50}; do
        curl -X POST http://127.0.0.1:27115/training/start
        sleep 0.1
        curl -X POST http://127.0.0.1:27115/training/cancel
        sleep 0.1
    done
    ```
4. Wait ~5 s for system to settle.
5. Re-record handle count (HC1) via Process Explorer.
6. Hash training_data.bin (H_after).

**Expected**:
- |HC1 - HC0| < 50 (some drift OK; no monotonic growth indicating per-cycle leak).
- H_before == H_after (no writes outside finalize per IPC-06 / D-23).
- /health still healthy (driver_loaded=true).
**Disposition**: ⬜ pending
**Evidence**: HC0, HC1, H_before, H_after.
**Operator notes**:

---

## Sign-Off

After all 10 cases reach PASS / N/A disposition:

- [ ] All 10 cases above marked PASS or N/A
- [ ] No FAIL dispositions (any FAIL blocks phase-complete; planner returns to a prior plan)
- [ ] `default.vrsettings` restored: `enable_driver_audio=false` AND `enable_driver_detection=false` (per CONTEXT D-40 — P10 owns flag flips)
- [ ] Pre-UAT backup `training_data.bin.preuat` deleted (no longer needed once stress integrity verified)
- [ ] Frontmatter updated: `tested: <date>`, `driver_sha: <SHA>`, `operator: <name>`, `status: completed`

**Operator signature**: <pending>
**Date**: <pending>

---

## Notes for Phase 10

If any of the following surfaced during UAT, note them for Phase 10 planning:
- Tray-icon glyph would clarify UI state during long training sessions (HEALTH-08).
- FAIL-* graceful failure UX gaps observed during Driver-Down case (D-39(6)).
- Default-flag flip readiness — does the system feel solid enough to ship `enable_driver_detection=true` by default?

These do NOT block Phase 9 sign-off; they inform Phase 10 scope.
```

Use Write tool to create the file. Replace `<YYYY-MM-DD>` placeholder for `created` with today's date (executor knows the date from context). Leave operator/SHA/result placeholders as `<pending>` — they get filled at test time.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-training-migration/09-UAT.md && grep -q "D-39(1)" .planning/phases/09-training-migration/09-UAT.md && grep -q "D-39(10)" .planning/phases/09-training-migration/09-UAT.md && grep -q "Sign-Off" .planning/phases/09-training-migration/09-UAT.md && grep -q "default.vrsettings" .planning/phases/09-training-migration/09-UAT.md && grep -q "training_data.bin" .planning/phases/09-training-migration/09-UAT.md && echo "09-UAT.md scaffold OK"</automated>
  </verify>
  <done>09-UAT.md exists with frontmatter (phase, type, created, tested, driver_sha, rig, operator, status), 10 case sections (D-39(1) through D-39(10)) with steps + expected + disposition + evidence + notes per case, and a Sign-Off section with default-flag-restore checklist (D-40). Placeholders for operator/SHA/result fields ready to be filled by the operator during the rig run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT on Bigscreen Beyond + Win11 Pro rig — execute D-39(1)..(10)</name>
  <what-built>
After 09-01 through 09-04 ship, Phase 9 production code is complete:
- Driver-side TrainingSession + training_io + DetectionRunner mode-branch (09-01)
- 5 new HTTP routes + IDriverApi + settings_validator extensions (09-02)
- Client UI rewire + AssertNoClientTraining go-live (09-03)
- WAV replay harness + dr_wav vendor + corpus + CI ctest (09-04)

The 10-case UAT regimen (CONTEXT D-39) is the gate. ROADMAP marks Phase 9 NEEDS VALIDATION specifically because the training UX commit/discard pattern was designed first-principles. Real-hardware sign-off transitions Phase 9 to "completed" status.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/09-training-migration/09-UAT.md` (created in Task 1).
2. Update frontmatter:
   - `tested: <today's date>`
   - `driver_sha: <output of git rev-parse --short HEAD>`
   - `rig.steamvr_version: <get from SteamVR's About panel>`
   - `rig.build_flavor: <Debug or RelWithDebInfo>`
   - `operator: <your name/email>`
3. Pre-UAT setup (per the document):
   - Build Phase 9 binaries from current HEAD.
   - Backup `%APPDATA%\MicMap\training_data.bin` → `training_data.bin.preuat` (for case 10 restoration).
   - Set `default.vrsettings`: `enable_driver_audio=true`, `enable_driver_detection=true` (UAT-only — restored at end).
   - Record pre-UAT hash: `sha256sum %APPDATA%\MicMap\training_data.bin > training_data_pre_uat.sha256`.
4. Execute cases D-39(1) through D-39(10) in order. For EACH case:
   - Follow the documented Steps verbatim.
   - Record actual outcome.
   - Mark disposition: PASS / FAIL / N/A.
   - Attach evidence (log excerpt or screenshot path) under the Evidence row.
   - Add operator notes if behavior deviated from expected.
5. **If any case FAILs**: stop. Report the failure to /gsd-execute-phase. Planner returns to the prior plan that owns the failing surface (e.g. case 5 timeout failure → 09-01 TrainingSession::tickTimeout; case 6 ECONNREFUSED → 09-03 client-side P8 gate inheritance). Do not mark Sign-Off.
6. **If all 10 cases PASS or N/A**:
   - Tick all checkboxes in the Sign-Off section.
   - Restore `default.vrsettings`: `enable_driver_audio=false`, `enable_driver_detection=false` (D-40 — flag flips belong to P10).
   - Delete `training_data.bin.preuat` (no longer needed).
   - Update frontmatter: `status: completed`.
   - Sign and date in the Operator signature field.
7. Resume `/gsd-execute-phase` and report `approved`.

**Rig metadata to record explicitly**:
- HMD model: Bigscreen Beyond (or whichever HMD the operator has — note any deviation).
- OS build: `winver` output.
- SteamVR version: Settings → About.
- Driver SHA: `git rev-parse --short HEAD`.
- Driver build: Debug or RelWithDebInfo (matters for the stress test — Debug shows handle leaks more clearly).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 9 complete, or describe any failing cases for planner re-engagement.</resume-signal>
</task>

</tasks>

<verification>
1. `.planning/phases/09-training-migration/09-UAT.md` exists with all 10 case sections + Sign-Off + frontmatter.
2. After human checkpoint completes:
   - All 10 cases marked PASS or N/A.
   - Sign-Off section ticked.
   - default.vrsettings restored to OFF flags (D-40).
   - Frontmatter `status: completed`, `tested: <date>`, `driver_sha: <SHA>`, `operator: <name>`.
3. ROADMAP.md "Phase 9 Training Migration" entry can flip from `[ ]` to `[x]` and gain a completion-date footnote.
</verification>

<success_criteria>
- 09-UAT.md scaffold created (Task 1)
- All 10 D-39 cases pass on Bigscreen Beyond + Win11 Pro (Task 2 — human checkpoint)
- default.vrsettings flags restored to OFF post-UAT (D-40)
- Phase 9 ready for /gsd-verify-work + ROADMAP update
- Next phase: P10 (Cutover & Cleanup) which owns the flag flips, POST /button deletion, FAIL cluster, HEALTH-08 tray glyphs, INST-09 installer co-versioning
</success_criteria>

<output>
After completion, create `.planning/phases/09-training-migration/09-05-SUMMARY.md` per the standard template. Document:
- UAT outcome (PASS / partial-pass with list of FAILed cases)
- Driver SHA tested
- Rig metadata (HMD / OS / SteamVR version / build flavor)
- Any anomalies surfaced that inform Phase 10 (e.g. visible UX gaps, stress-test handle drift trends, corpus determinism edge cases)
- Confirmation of default.vrsettings restoration
</output>
