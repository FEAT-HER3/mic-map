---
phase: 10-cutover-cleanup
plan: 07
type: execute
wave: 7
depends_on: ["10-06"]
files_modified:
  - .planning/phases/10-cutover-cleanup/10-UAT.md
  - CLAUDE.md
autonomous: false
requirements: [MIG-05, FAIL-01, FAIL-02, FAIL-03, FAIL-04, FAIL-05, HEALTH-08, TEST-01, TEST-02, TEST-03, TEST-05, INST-09]
tags: [phase-10, cutover-cleanup, wave-7, uat, hardware-loop, manual-checkpoint, claude-md-update]

must_haves:
  truths:
    - "Real-hardware UAT regimen D-25(1)..(15) executed on Bigscreen Beyond + Win11 Pro rig with operator sign-off"
    - "All 15 D-25 cases recorded in 10-UAT.md with PASS/FAIL/N/A disposition + driver SHA + rig metadata + operator name + date"
    - "Cutover smoke (D-25(1)): install fresh build, launch SteamVR, cover mic → dashboard toggles via driver-resident detection path; ZERO HTTP POST /button traffic verified (synthetic /button check returns 404)"
    - "Tray glyph state transitions (D-25(2)): armed-green idle → triggered-pulse on cover-mic → back to armed within 300ms+cooldown; inject error (kill SteamVR) → tray red within 1 health-poll cycle (≤1s); explorer.exe restart sub-test (Pitfall 1) validates WM_TASKBAR_CREATED handler — kill explorer via Task Manager, observe tray icon reappears"
    - "FAIL-01 mic-permission (D-25(3)): revoke mic permission in Win11 Privacy → pill 'Mic access blocked' surfaces; click button → ms-settings:privacy-microphone opens; re-grant → pill clears within one poll"
    - "FAIL-02 driver-missing (D-25(4)): stop SteamVR, rename driver_micmap.dll, restart SteamVR → client pill 'Driver not installed'; tray red"
    - "FAIL-03 SteamVR-not-running (D-25(5)): stop SteamVR while client running → pill 'SteamVR not running'; verify 1Hz poll cadence (no retry storm) via netstat / packet capture; restart SteamVR → pill clears, tray green"
    - "FAIL-04 double-instance (D-25(6)): launch micmap.exe twice → second instance foregrounds the first and exits silently; only one process in Task Manager; window restored from minimized if applicable"
    - "FAIL-05 device-removed (D-25(7)): unplug USB mic mid-session (or disable via Device Manager) → pill 'Microphone disconnected'; tray red; reconnect → driver IMMNotificationClient rebinds → pill auto-clears, tray green"
    - "--debug-trigger end-to-end (D-25(8)): Debug build `micmap.exe --debug-trigger` exits 0; HMD dashboard toggles; Release build `micmap.exe --debug-trigger` exits non-zero (route 404)"
    - "Log rotation (D-25(9)): synthesize 6MB of log writes (verbose Debug build); verify micmap-driver.log < 5MB after rotation; .log.{1..5} exist; .log.6 does not"
    - "Version mismatch warning (D-25(10)): install matched set vX.Y.Z; manually replace one binary with vX.Y.Z-prev; restart client → WARNING in micmap.log; pill 'Version mismatch' surfaces; detection still works (pill is warn-only per D-20)"
    - "Installer round-trip on clean Win11 VM (D-25(11)): install vX.Y.Z → upgrade vX.Y.Z+1 → uninstall; vrpathreg show no longer lists MicMap; <Steam>/drivers/micmap removed; %APPDATA%/MicMap left in place per CFG-04 user-data preservation"
    - "Binary size regression (D-25(12)): micmap.exe size dropped noticeably vs pre-cutover commit; delta documented in 10-05-SUMMARY (and re-confirmed here in 10-UAT.md)"
    - "SVR-05 grep audit (D-25(13)): `grep -rn 'VRDriverInput\\|VRProperties\\|VRServerDriverHost' driver/src/` returns hits ONLY in device_provider.cpp and manifest_registrar.cpp"
    - "HMD sleep/wake stress (D-25(14)): 50 cycles of HMD wake/sleep with detection running; no leaked handles in Process Explorer; vrserver.txt clean"
    - "Cutover lint go-live verification (D-25(15)): all 3 new lints fire on a synthetic regression (deliberately reintroduce a `POST /button` registration / a `detector->analyze()` in apps/micmap/); build fails; revert; build passes"
    - "CLAUDE.md 'Hardware rig' section updated to reflect post-cutover defaults (enable_driver_audio + enable_driver_detection both true; D-02 emergency-override-only-per-install caveat documented per RESEARCH §Pitfall 9 acceptance)"
    - "NO post-UAT default-OFF restore — P10 OWNS the flip per CONTEXT D-25 (in contrast to P6/P7/P8/P9 which restored to OFF post-UAT)"
  artifacts:
    - path: ".planning/phases/10-cutover-cleanup/10-UAT.md"
      provides: "Manual UAT sign-off document with 15 case rows, PASS/FAIL/N/A disposition, driver SHA, rig metadata, operator name, dates"
      contains: "D-25"
    - path: "CLAUDE.md"
      provides: "'Hardware rig' section reflects post-cutover state (both flags true; emergency-override-per-install caveat noted)"
      contains: "enable_driver_audio"
  key_links:
    - from: ".planning/phases/10-cutover-cleanup/10-UAT.md"
      to: ".planning/phases/10-cutover-cleanup/10-CONTEXT.md"
      via: "15-case UAT regimen mirrors D-25 in CONTEXT verbatim"
      pattern: "D-25"
    - from: "CLAUDE.md"
      to: ".planning/phases/10-cutover-cleanup/10-CONTEXT.md"
      via: "Hardware rig section updates per D-25 + Pitfall 9 acceptance"
      pattern: "enable_driver_audio"
---

<objective>
Wave 7: Manual UAT on real hardware. Phase 10 cannot ship without a Bigscreen Beyond + Win11 Pro pass on the 15 D-25 cases. Plus: update CLAUDE.md "Hardware rig" section to reflect post-cutover defaults (since P10 OWNS the flip per CONTEXT D-25 — there's NO post-UAT default-OFF restore).

This plan has TWO tasks:
1. Scaffold `10-UAT.md` from the 15-case D-25 regimen + the operator+SHA+date metadata convention from P7/P8/P9.
2. PAUSE at a `checkpoint:human-verify` task while the operator runs the regimen on the rig and records results. After sign-off, update CLAUDE.md "Hardware rig" section to reflect post-cutover defaults.

Plan boundaries:
- IN: 10-UAT.md scaffold creation; manual checkpoint for the 15-case regimen; CLAUDE.md update post-sign-off.
- OUT: any code changes (the prior 6 plans cover all production code). UAT is pure verification + docs update.
- INHERIT: P6/P7/P8/P9 UAT regimen shape (operator+SHA+date metadata, per-case row, PASS/FAIL disposition).

Why autonomous: false — this plan contains a real-hardware checkpoint that must pause for human action.
</objective>

<execution_context>
@$HOME/.ccs/instances/bigscreen/get-shit-done/workflows/execute-plan.md
@$HOME/.ccs/instances/bigscreen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cutover-cleanup/10-CONTEXT.md
@.planning/phases/10-cutover-cleanup/RESEARCH.md
@.planning/phases/10-cutover-cleanup/10-00-PLAN.md
@.planning/phases/10-cutover-cleanup/10-01-PLAN.md
@.planning/phases/10-cutover-cleanup/10-02-PLAN.md
@.planning/phases/10-cutover-cleanup/10-03-PLAN.md
@.planning/phases/10-cutover-cleanup/10-04-PLAN.md
@.planning/phases/10-cutover-cleanup/10-05-PLAN.md
@.planning/phases/10-cutover-cleanup/10-06-PLAN.md
@.planning/phases/06-driver-side-audio-capture-spike/06-UAT.md
@.planning/phases/08-ipc-contract-reshape/08-UAT.md
@.planning/phases/09-training-migration/09-UAT.md
@CLAUDE.md

<interfaces>
<!-- Prior UAT documents to mirror in shape. Executor reads before scaffolding 10-UAT.md. -->

P9 UAT shape (`.planning/phases/09-training-migration/09-UAT.md` — most recent shipped UAT — most directly applicable):
- Frontmatter with phase / created / tested / driver_sha / rig metadata / operator placeholders
- Per-case rows in a table or H3 sections: case ID, description, expected outcome, actual outcome, disposition, evidence (logs/screenshots)
- Final approval line

CONTEXT D-25 — 15-case regimen verbatim (excerpted; full text in 10-CONTEXT.md):
1. Cutover smoke
2. Tray glyph state transitions (+ explorer-restart sub-test for Pitfall 1)
3. FAIL-01 mic-permission
4. FAIL-02 driver-missing
5. FAIL-03 SteamVR-not-running
6. FAIL-04 double-instance
7. FAIL-05 device-removed
8. --debug-trigger end-to-end
9. Log rotation
10. Version mismatch warning
11. Installer round-trip on clean Win11 VM
12. Binary size regression
13. SVR-05 grep audit
14. HMD sleep/wake stress
15. Cutover lint go-live verification

Bigscreen Beyond rig metadata (typical fields — operator fills):
- HMD: Bigscreen Beyond
- OS: Windows 11 Pro
- SteamVR version: e.g. 2.5.x
- Driver SHA: from `git rev-parse --short HEAD` at start of UAT
- Operator: name/email
- Test date: ISO 8601
- Driver build flavor: Debug or RelWithDebInfo (D-25(8) requires both Debug and Release builds for the --debug-trigger comparison; D-25(9) needs Debug for verbose logging; most other cases work in either)

CLAUDE.md "Hardware rig" section — current text (verified per repo):
- Lists Bigscreen Beyond + Win11 Pro UAT rig
- Backup convention (.preuat.bak)
- Restore expectation: "D-40 demands enable_driver_audio=false AND enable_driver_detection=false after sign-off"

The P10 update REVERSES this. After 10-07 sign-off, the section MUST read approximately:
- D-40 superseded by P10 D-25 — NO post-UAT default-OFF restore
- enable_driver_audio + enable_driver_detection both ship as `true` post-Phase 10 (the on-rig install becomes the new shipped default)
- D-02 emergency-override-per-install caveat: a user CAN edit default.vrsettings to false post-install, but installer upgrades will overwrite (Pitfall 9 acceptance — this is install-scoped emergency override, not a runtime config knob)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → 10-UAT.md sign-off | Trusted single operator; sign-off is the gate |
| real hardware → driver runtime | Operator interacts with physical mic + HMD; observed behavior is the ground truth |
| HTTP localhost endpoints (curl) → driver | Validates 200/404 envelope shapes hands-on |
| Process Explorer → vrserver / micmap.exe | Verifies no leaked handles in stress tests |
| Clean Win11 VM (D-25(11)) → installer | Validates atomic install/upgrade/uninstall cycles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Repudiation | UAT pass-disposition without evidence | mitigate | Each PASS row in 10-UAT.md must cite a specific evidence artifact (log line, screenshot path, vrserver.txt fragment, ctest output, etc.); empty evidence = N/A or FAIL |
| T-10-07-02 | DoS | UAT regimen too long → operator skips cases | mitigate | 15 cases is bounded (P7=6, P8=7, P9=10 — P10 is the largest UAT due to coverage scope); each case is independently scorable; estimated 2-4 hours of operator time |
| T-10-07-03 | Spoofing | crafted /health response from a tampered driver passes UAT | accept | Operator's UAT runs against the freshly-built driver from the post-cutover commit; not a 3rd-party-supplied binary |
| T-10-07-04 | Information disclosure | UAT logs contain mic-cover audio metadata | accept | RMS / state names only; no raw audio in logs (per P6/P7 design); operator may sanitize before commit |
| T-10-07-05 | Tampering | post-UAT someone reverts the flag flip | mitigate | The flip lives in driver/resources/settings/default.vrsettings (in repo); reverting requires a commit that AssertNoButtonRoute / AssertNoClientDetection won't catch (those guard the rip-out, not the flag); but ROADMAP / CLAUDE.md updates document the intent loudly |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 10-UAT.md from D-25(1)..(15) with operator sign-off table + rig metadata frontmatter</name>
  <files>.planning/phases/10-cutover-cleanup/10-UAT.md</files>
  <read_first>
    - .planning/phases/09-training-migration/09-UAT.md (most recent UAT — directly applicable shape)
    - .planning/phases/08-ipc-contract-reshape/08-UAT.md (sibling reference)
    - .planning/phases/07-driver-side-detection-thread/07-UAT.md (sibling reference if it exists)
    - .planning/phases/10-cutover-cleanup/10-CONTEXT.md decision D-25 (verbatim 15-case regimen)
    - .planning/phases/10-cutover-cleanup/RESEARCH.md "Pitfalls Index Wave 7" (sub-step suggestions for Pitfalls 1, 2, 8 within the existing D-25 cases)
  </read_first>
  <action>
Create .planning/phases/10-cutover-cleanup/10-UAT.md. Use Write tool. Mirror the P9 UAT shape verbatim with these adjustments:

**Frontmatter:**
```yaml
---
phase: 10-cutover-cleanup
created: <today's date>
tested: <PLACEHOLDER>
driver_sha: <PLACEHOLDER — populate via `git rev-parse --short HEAD` at UAT start>
operator: <PLACEHOLDER — name/email>
rig:
  hmd: Bigscreen Beyond
  os: Windows 11 Pro
  steamvr_version: <PLACEHOLDER — e.g. 2.5.x>
  build_flavor: Debug + RelWithDebInfo (both required for D-25(8) and D-25(9))
status: in-progress
---
```

**Body sections** — one section per D-25 case. Use this template:

```markdown
## D-25(N) — <case title>

**Spec:** <one-paragraph description from CONTEXT D-25(N), verbatim>

**Steps:**
1. <step 1>
2. <step 2>
3. <step 3>

**Expected:**
- <observable outcome 1>
- <observable outcome 2>

**Actual:** <PLACEHOLDER — operator fills>

**Evidence:** <PLACEHOLDER — log lines, screenshot paths, ctest output, etc.>

**Disposition:** <PLACEHOLDER — PASS / FAIL / N/A with reason>
```

**Per-case content** (15 sections):

### D-25(1) — Cutover smoke
**Spec:** Install fresh build, launch SteamVR, cover mic → dashboard toggles. Driver-side detection is the trigger path (verify ZERO HTTP `POST /button` traffic via Wireshark or a synthetic /button 404 check).
**Steps:**
1. Build the driver+client from current HEAD; install via `MicMap-Setup-v1.6.0.exe`.
2. Launch SteamVR; verify driver loads (vrserver.txt shows `driver_micmap` Init OK).
3. Cover the mic with hand; observe SteamVR dashboard toggles.
4. Run `curl -X POST http://127.0.0.1:27015/button` (synthetic) — expect HTTP 404 (route deleted in 10-05).
5. Re-cover mic to verify driver-resident detection still toggles.
**Expected:** Dashboard toggles via driver path; /button returns 404.

### D-25(2) — Tray glyph state transitions
**Spec:** Observe armed-green at idle → triggered-pulse on cover-mic → back to armed-green within 300ms + cooldown. Inject error: kill SteamVR → tray turns red within 1 health-poll cycle (≤1s). Sub-test (Pitfall 1): kill explorer.exe via Task Manager, observe tray icon reappears (WM_TASKBAR_CREATED handler).
**Steps:**
1. Launch micmap.exe; observe tray icon = armed-green.
2. Cover mic; observe tray flashes triggered-pulse for ~300ms then reverts to armed.
3. Stop SteamVR via SteamVR menu; within ≤1s observe tray = error-red.
4. Restart SteamVR; observe tray returns to armed-green within ≤1s.
5. (Pitfall 1 sub-test) With micmap.exe running, kill explorer.exe via Task Manager; explorer auto-restarts; observe tray icon reappears (does NOT vanish permanently).
6. Open Process Explorer; observe GDI handle count for micmap.exe stays bounded across 10 state transitions (Pitfall 2 verification).
**Expected:** All 3 glyphs render correctly; pulse window ~300ms; error-state within poll cycle; explorer-restart recovery; no GDI leak.

### D-25(3) — FAIL-01 mic-permission
**Spec:** Revoke mic permission in Win11 Privacy settings while client is running. Pill surfaces "Mic access blocked"; click button → Windows Settings opens at `ms-settings:privacy-microphone`. Re-grant → pill clears within one poll.
**Steps:**
1. Launch micmap.exe; verify driver-health pane shows no FAIL pill.
2. Open Win11 Settings → Privacy & Security → Microphone; toggle "Microphone access" off.
3. Within one poll cycle (~1s), observe pill "Mic access blocked" with "Open Windows mic settings" button.
4. Click the button; verify Windows Settings opens at the microphone privacy page.
5. Re-enable microphone access; within one poll cycle, pill clears.
**Expected:** Pill surfaces correctly; deep-link works; auto-clear on resolution.

### D-25(4) — FAIL-02 driver-missing
**Spec:** Stop SteamVR, rename `driver_micmap.dll`, restart SteamVR. Client pill: "Driver not installed". Tray red.
**Steps:**
1. Stop SteamVR.
2. Rename `<Steam>\steamapps\common\SteamVR\drivers\micmap\bin\win64\driver_micmap.dll` to `.bak`.
3. Restart SteamVR.
4. Launch micmap.exe (or observe if already running); within one poll cycle, observe pill "Driver not installed — run installer or enable in SteamVR" + "Open SteamVR" button; tray red.
5. Click "Open SteamVR" button; verify steam://rungameid/250820 launches SteamVR client (or no-op if already up).
6. Restore the DLL; restart SteamVR; pill clears.
**Expected:** Correct pill text; deep-link works; tray red.

### D-25(5) — FAIL-03 SteamVR-not-running (cold-start scope, v1.6)
**Spec amended (Phase 10 UAT, 2026-05-10):** Per-client-survives-SteamVR-exit was the original intent; UAT showed `vrInput`'s `VREvent_Quit` handler exits the client when vrserver dies mid-session, so the mid-session pill path is unreachable for v1.6. Reframed to **cold-start-only**: launch micmap.exe with SteamVR not running; pill "SteamVR not running" surfaces (no action button per fail_pill.cpp — no canonical launch URI); start SteamVR → driver loads → pill clears. Mid-session SteamVR-exit survival is carried over to **P11 D-N1 (client paired-life with SteamVR — survive-restart)** with the existing P10-carryover backlog.
**Steps:**
1. With SteamVR NOT running, launch micmap.exe.
2. Within one poll cycle (~1s), observe red FAIL-03 pill text "SteamVR not running" between the Driver Health header and the P8 status block.
3. Start SteamVR via Steam → SteamVR; wait for vrserver + driver load.
4. Within ~2s of driver load, pill clears; status flips to SteamVR: Connected (green) + Driver: Connected (green).
**Expected:** FAIL-03 disambiguation correct (vrserverRunning=false → SteamVRNotRunning, NOT DriverNotLoaded); auto-recovery on SteamVR launch.
**Out of scope (v1.6):** Mid-session `vrserver` kill while client running — client exits via `VREvent_Quit` (see micmap.log "[INFO] SteamVR quit event received"); not a regression, original P10 design did not include client survive-restart. See P11 carryover.

### D-25(6) — FAIL-04 double-instance
**Spec:** Launch micmap.exe twice. Second instance foregrounds the first and exits silently (verify via Task Manager — only one process). Window is restored from minimized if applicable.
**Steps:**
1. Launch micmap.exe via desktop shortcut or Start menu.
2. Minimize the window to tray.
3. Launch micmap.exe a second time the same way.
4. Observe: second instance does NOT open a new window; first instance restores from tray and comes to foreground.
5. Open Task Manager; verify only ONE micmap.exe process exists.
**Expected:** Single-instance enforcement via mutex; window restore; silent second-instance exit.

### D-25(7) — FAIL-05 device-removed
**Spec:** Unplug USB mic mid-session (or use Device Manager to disable). Pill: "Microphone disconnected"; tray red. Reconnect → driver IMMNotificationClient callback rebinds → pill auto-clears, tray green.
**Steps:**
1. With SteamVR running and driver-resident detection active, verify mic capture is working (cover mic → dashboard toggles).
2. Unplug USB mic (or Device Manager → Disable).
3. Within one poll cycle, observe pill "Microphone disconnected — reconnect to resume detection"; tray red.
4. Plug mic back in (or Enable in Device Manager).
5. Within ~2s (driver IMMNotificationClient rebind window), pill auto-clears; tray green.
**Expected:** Pill surfaces; auto-recovery on reappear (no manual action required).

### D-25(8) — `--debug-trigger` end-to-end (Debug + Release builds)
**Spec:** Debug build `micmap.exe --debug-trigger` exits 0; HMD dashboard toggles. Release build: same command exits non-zero (route 404).
**Steps:**
1. Build a Debug `micmap.exe`; install over the existing one.
2. With SteamVR running, run `micmap.exe --debug-trigger` from a terminal; verify exit code 0; verify HMD dashboard toggles.
3. Build a Release `micmap.exe`; install over the existing one.
4. Run `micmap.exe --debug-trigger`; verify exit code 1 (HTTP 404 — `/debug/trigger` route is `#if MICMAP_DEBUG_BUILD`-gated and not registered in Release).
**Expected:** Debug = exit 0 + dashboard toggle; Release = exit 1.

### D-25(9) — Log rotation
**Spec:** Synthesize 6MB of log writes (e.g., temporary debug-build with verbose logging). Verify rotation: micmap-driver.log < 5MB after rotation; micmap-driver.log.{1..5} exist; .6 does not.
**Steps:**
1. Build a Debug driver with verbose logging enabled (e.g., set DETECTION_RMS_LOG / verbose flags).
2. Install + run; let detection run for ~10 minutes (or use --debug-trigger in a tight loop) to generate >5MB of log lines.
3. After log file size exceeds 5MB, observe rotation: `dir %APPDATA%\MicMap\micmap-driver.log*` shows .log + .log.1.
4. Continue logging until 5+ rotations have occurred.
5. Verify: `micmap-driver.log` < 5MB; `.log.1` through `.log.5` exist; `.log.6` does NOT exist (oldest dropped per D-14).
6. Verify no `*.tmp` files leftover.
**Expected:** 5MB cap honored; 5-generation cap enforced; atomic move (no .tmp leftovers).

### D-25(10) — Version mismatch warning
**Spec:** Install matched set (driver + client both `vX.Y.Z`). Replace one binary with `vX.Y.Z-prev` (manually). Restart client. WARNING in `micmap.log`; pill "Version mismatch" surfaces; detection still works.
**Steps:**
1. Install MicMap-Setup-v1.6.0.exe (matched set).
2. Build a v1.5.0 (or any prior) `micmap.exe`; manually copy over the installed `micmap.exe`.
3. Restart SteamVR (driver still v1.6.0); launch the v1.5.0 client.
4. Verify `micmap.log` contains a WARNING line: `driver version '1.6.0' does not match client '1.5.0'`.
5. Verify driver-health pane shows the version-mismatch pill (amber, "Version mismatch — driver v1.6.0 vs client v1.5.0 — reinstall recommended", with Dismiss button).
6. Cover mic; verify dashboard STILL toggles (D-20: warn-only, never blocks).
7. Click Dismiss; pill clears for the rest of the session.
**Expected:** WARNING + pill + non-blocking detection; Dismiss works.

### D-25(11) — Installer round-trip on clean Win11 VM
**Spec:** Install vX.Y.Z → upgrade to vX.Y.Z+1 → uninstall. After uninstall, vrpathreg show does not list MicMap; <Steam>/drivers/micmap removed; %APPDATA%/MicMap left in place per CFG-04 user-data preservation.
**Steps:**
1. On a clean Win11 VM (no prior MicMap install): run `MicMap-Setup-v1.6.0.exe`. Verify install succeeds elevated; SteamVR registration via vrpathreg.
2. Verify the 3 tray .ico files are present at `<Steam>\steamapps\common\SteamVR\drivers\micmap\bin\resources\` (Pitfall 8 mitigation).
3. Build a `MicMap-Setup-v1.6.1.exe` (bump cmake/version.cmake); run upgrade; verify install succeeds atomically (Inno Setup [Files] all-or-nothing).
4. Run "Uninstall" via Windows Apps; verify `vrpathreg show` no longer lists MicMap; verify `<Steam>/drivers/micmap/` is removed.
5. Verify `%APPDATA%/MicMap/` is preserved (training_data.bin + config.json remain — CFG-04).
**Expected:** Atomic install/upgrade/uninstall; user data preserved; .ico files present in install dir.

### D-25(12) — Binary size regression
**Spec:** micmap.exe size dropped noticeably vs pre-cutover commit. Document delta.
**Steps:**
1. Reference 10-05-SUMMARY for PRE-CUTOVER and POST-CUTOVER size measurements.
2. On the rig, build the current HEAD micmap.exe (Release config for fair comparison if 10-05 measured Debug, or Debug if 10-05 measured Debug — match).
3. Compare; document delta in this case row + cite 10-05-SUMMARY.
4. Per Pitfall 7: delta may be modest (~10-30KB) because KissFFT remains transitively linked. Acceptable per D-03 SOFT check.
**Expected:** Delta documented; Pitfall 7 caveat acknowledged.

### D-25(13) — SVR-05 grep audit
**Spec:** `grep -rn 'VRDriverInput\\|VRProperties\\|VRServerDriverHost' driver/src/` returns hits ONLY in device_provider.cpp and manifest_registrar.cpp.
**Steps:**
1. Run the grep:
   ```bash
   grep -rn 'VRDriverInput\|VRProperties\|VRServerDriverHost' driver/src/
   ```
2. Inspect output; verify ALL hits are in `device_provider.cpp` or `manifest_registrar.cpp`; NO other TU touches `vr::*`.
3. Specifically verify: `detection_runner.cpp`, `audio_worker.cpp`, `http_server.cpp`, `training_session.cpp`, `training_io.cpp`, `config_io.cpp`, `settings_validator.cpp`, `command_queue.hpp` are all clean.
**Expected:** Grep audit confirms SVR-05 invariant survives the cutover (the v1.5 SVR-05 pillar that survives every milestone).

### D-25(14) — HMD sleep/wake stress
**Spec:** 50 cycles of HMD wake/sleep with detection running. No leaked handles in Process Explorer; vrserver.txt clean.
**Steps:**
1. With detection active (cover mic → dashboard toggles working), open Process Explorer → micmap.exe + vrserver.exe → record initial Handle count + GDI count.
2. Cycle HMD: take HMD off (proximity sensor triggers Standby) → wait 5s → put HMD on → wait 5s. Repeat 50 times.
3. After 50 cycles, observe Handle counts in Process Explorer; verify NO unbounded growth (small fluctuations OK; 1000+ handle increase = leak).
4. Inspect `vrserver.txt` for the test window; verify no error / leak warnings related to micmap.
5. Verify detection still works after the 50 cycles (cover mic → dashboard toggles).
**Expected:** No handle leaks; vrserver.txt clean; detection survives.

### D-25(15) — Cutover lint go-live verification
**Spec:** All 3 new lints fire on a synthetic regression (deliberately reintroduce a `POST /button` registration / a `detector->analyze()` in `apps/micmap/`); build fails. Revert; build passes.
**Steps:**
1. Synthetic regression A — re-add `POST /button` to driver/src/http_server.cpp:
   ```cpp
   srv.Post("/button", [](const httplib::Request&, httplib::Response& res) { res.status = 200; });
   ```
   Run `ctest --test-dir build -R AssertNoButtonRoute --output-on-failure`. Expect FATAL.
2. Revert; verify ctest passes.
3. Synthetic regression B — re-add `detector->analyze()` in apps/micmap/main.cpp (anywhere; add a stub):
   ```cpp
   // synthetic
   void __reg() { INoiseDetector* d = nullptr; if (d) d->analyze(nullptr, 0); }
   ```
   Run `ctest --test-dir build -R AssertNoClientDetection --output-on-failure`. Expect FATAL.
4. Revert; verify ctest passes.
5. Synthetic regression C — corrupt cmake/version.cmake to set MICMAP_VERSION="1.7.0" but leave installer/version.iss showing "1.6.0":
   ```bash
   sed -i 's/MICMAP_VERSION "1.6.0"/MICMAP_VERSION "1.7.0"/' cmake/version.cmake
   cmake -B build -S .   # regenerates version.iss as 1.7.0 — this matches; force a stale state by hand-editing version.iss back to 1.6.0
   ```
   Run `ctest --test-dir build -R AssertCoVersioning --output-on-failure`. Expect FATAL (drift detected).
6. Revert all changes; verify all 3 lints PASS.
**Expected:** All 3 lints catch their respective regressions; revert clears.

**Final approval:**
```markdown
---

## Sign-off

**Operator:** _______________________
**Date:** _______________________
**Status:** All 15 cases PASS (or document FAIL/N/A reasoning above per case)

By signing, operator confirms Phase 10 cutover meets all 6 success criteria from ROADMAP §"Phase 10: Cutover & Cleanup".
```

Mark the file as `status: in-progress` in frontmatter; the operator updates to `status: signed` post-signoff.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-cutover-cleanup/10-UAT.md && grep -q "D-25(1)" .planning/phases/10-cutover-cleanup/10-UAT.md && grep -q "D-25(15)" .planning/phases/10-cutover-cleanup/10-UAT.md && grep -q "Bigscreen Beyond" .planning/phases/10-cutover-cleanup/10-UAT.md && grep -q "operator:" .planning/phases/10-cutover-cleanup/10-UAT.md && grep -cE "^## D-25\\([0-9]+\\)" .planning/phases/10-cutover-cleanup/10-UAT.md | grep -q "^15$" && echo "10-UAT.md scaffold OK with all 15 cases"</automated>
  </verify>
  <done>10-UAT.md exists with frontmatter (rig + operator + driver_sha placeholders) + 15 case sections following the P9 UAT shape (D-25(1)..(15) verbatim from CONTEXT). Each section has Spec / Steps / Expected / Actual (placeholder) / Evidence (placeholder) / Disposition (placeholder). Final sign-off block with operator + date + status. status: in-progress until operator signs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 10 cutover landed across waves 0-6: log rotation (10-01), version SSoT (10-01), tray glyphs (10-02), FAIL pills + named-mutex hardening + driver_version /health field (10-03), --debug-trigger end-to-end (10-04), atomic cutover commit deleting POST /button + IDriverApi::tap() + ~500 LoC client-side detection body + flipping default.vrsettings to true (10-05), installer co-versioning + version-mismatch pill (10-06).

12 lints all GREEN. All test_* ctests pass. Driver + client + mic_test + hmd_button_test build cleanly.

This checkpoint is the real-hardware UAT regimen on Bigscreen Beyond + Win11 Pro per CONTEXT D-25 (15 cases). The 10-UAT.md scaffold from Task 1 is the recording artifact.
  </what-built>
  <how-to-verify>
**Operator workflow:**

1. **Pre-UAT setup:**
   - Build current HEAD: `cmake -B build -S . -DCMAKE_BUILD_TYPE=Debug && cmake --build build`
   - Build the installer: `cmake --build build --target package` (produces `MicMap-Setup-v1.6.0.exe`)
   - Backup existing installs:
     - `cp <Steam>/drivers/micmap/bin/win64/driver_micmap.dll <Steam>/.../driver_micmap.dll.preuat.bak`
     - `cp <Steam>/drivers/micmap/bin/micmap.exe <Steam>/.../micmap.exe.preuat.bak`
     - `cp %APPDATA%/MicMap/training_data.bin %APPDATA%/MicMap/training_data.bin.preuat.bak`
   - Capture driver SHA: `git rev-parse --short HEAD` — record in 10-UAT.md frontmatter.

2. **Execute the 15 cases** in `10-UAT.md`:
   - Read each case's Spec / Steps / Expected.
   - Perform on the rig.
   - Record Actual + Evidence + Disposition in the case section.

3. **Per-case pacing:** estimated 10-15 min/case for quick cases (D-25(1), (3), (6), (8), (13)); 30-45 min/case for deeper ones (D-25(2) tray + Pitfall 1 sub-test, D-25(11) clean-VM round-trip, D-25(14) 50-cycle stress, D-25(15) lint go-live with revert/redo). Total estimated 4-6 hours.

4. **Sign off** at the bottom of 10-UAT.md:
   - Fill in operator name + date + status: signed
   - All cases PASS → phase complete; report "approved" to resume execute-plan
   - Any FAIL → document the failure, attach evidence, report blocker; do NOT mark signed

5. **Post-UAT cleanup:**
   - Per CONTEXT D-25: NO post-UAT default-OFF restore (in contrast to P6/P7/P8/P9 — P10 OWNS the flip; the on-rig install becomes the new shipped default).
   - Restore the .preuat.bak files only if a FAIL needs investigation (otherwise leave the post-cutover state in place — it IS the new default).

6. **Resume signal:** Type "approved" if all 15 cases PASS. Otherwise type "blocked: <case> - <reason>" and the orchestrator will pause for triage.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 15 D-25 cases PASS and 10-UAT.md is signed. Otherwise type "blocked: D-25(N) - <reason>" with the failing case + reason.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md "Hardware rig" section to reflect post-cutover defaults (NO post-UAT default-OFF restore)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current "Hardware rig" section — text references D-40 demanding enable_driver_audio=false AND enable_driver_detection=false post-sign-off)
    - .planning/phases/10-cutover-cleanup/10-CONTEXT.md decisions D-01, D-02, D-25 (NO post-UAT restore)
    - .planning/phases/10-cutover-cleanup/RESEARCH.md "Pitfall 9" (install-time overwrite of user-edited values; install-scoped emergency override)
  </read_first>
  <action>
Modify CLAUDE.md "Hardware rig" section. Use Edit tool. Do NOT touch other sections.

Locate the "Hardware rig (this machine)" section. Find the sentence(s) referencing D-40 and the requirement to restore default.vrsettings to false post-UAT. Replace with the post-Phase-10 reality:

**Old text** (search for):
```
…D-40 demands `enable_driver_audio=false` AND `enable_driver_detection=false` after sign-off.
```

**New text:**
```
**Post-Phase-10 default state** (ships as TRUE):
- `enable_driver_audio: true` AND `enable_driver_detection: true` are the new shipped defaults per Phase 10 D-25 (P10 OWNS the flip; supersedes the P6-P9 D-40 post-UAT-OFF discipline).
- The driver runs detection end-to-end by default; client is settings + driver-health UI only post-cutover.
- D-02 emergency override: a user CAN edit `default.vrsettings` to `false` for debugging, but installer upgrades will overwrite (Pitfall 9 acceptance — install-scoped, not a runtime config knob).
- The on-rig install at `<Steam>/drivers/micmap/` is the shipped reference for the post-cutover defaults; restore from `driver/resources/settings/default.vrsettings` (canonical pristine source) for clean-state debugging.
```

Adjust phrasing to fit the existing paragraph flow. The key informational additions:
- Both flags ship as TRUE post-Phase 10
- D-40 no longer applies
- D-02 emergency override caveat (install-scoped per Pitfall 9)
- Pristine source location reference unchanged

If the existing CLAUDE.md text references the bak-files pattern (e.g., `.preuat.bak`), preserve that pattern but note the restore is now optional (used only for FAIL investigation, not as routine post-UAT cleanup).

**Anti-patterns prohibited:**
- Do NOT remove the rest of the "Hardware rig" section (driver paths, build copy paths, HTTP port, %APPDATA% notes — all stay).
- Do NOT add a "rollback procedure" section — D-02 emergency override is a single-key edit; not a procedure.
- Do NOT silently delete the D-40 reference — explicitly note that it's superseded by D-25 (so future readers know to look at the most recent decision).
  </action>
  <verify>
    <automated>grep -q "Phase 10\|Phase-10\|P10" CLAUDE.md && grep -q "enable_driver_audio.*true\|enable_driver_audio: true" CLAUDE.md && grep -q "D-25\|D-02\|Pitfall 9" CLAUDE.md && ! grep -q "D-40 demands.*enable_driver_audio=false" CLAUDE.md && echo "CLAUDE.md Hardware rig updated"</automated>
  </verify>
  <done>CLAUDE.md "Hardware rig" section reflects post-Phase-10 reality: both flags ship as TRUE; D-40 superseded by D-25 (no post-UAT default-OFF restore); D-02 emergency-override-per-install caveat noted; existing driver paths / build paths / HTTP port / %APPDATA% references preserved.</done>
</task>

</tasks>

<verification>
1. .planning/phases/10-cutover-cleanup/10-UAT.md exists with all 15 D-25 case sections, frontmatter (operator/SHA/rig placeholders), and final sign-off block.
2. Operator executes all 15 cases on Bigscreen Beyond + Win11 Pro rig; records PASS/FAIL/N/A + evidence per case.
3. All 15 cases PASS; 10-UAT.md status: signed with operator name + date.
4. CLAUDE.md "Hardware rig" section updated to reflect post-cutover defaults (both flags TRUE; D-40 superseded by D-25; D-02 emergency-override-per-install caveat).
5. NO post-UAT default-OFF restore — on-rig install IS the new shipped default per CONTEXT D-25.
</verification>

<success_criteria>
- 10-UAT.md scaffold with all 15 D-25 cases (Task 1)
- Operator sign-off completed; all 15 cases PASS (Task 2 checkpoint)
- CLAUDE.md "Hardware rig" updated post-sign-off (Task 3)
- Phase 10 declared complete; ROADMAP.md status row flips to "Complete"
</success_criteria>

<output>
After completion, create `.planning/phases/10-cutover-cleanup/10-07-SUMMARY.md`. Document: actual operator name + sign-off date; any case-level FAIL/N/A dispositions with reasons; the post-cutover micmap.exe size confirmation (cite 10-05-SUMMARY); the actual MicMap-Setup-vX.Y.Z.exe artifact name + path; confirmation that CLAUDE.md "Hardware rig" was updated; final phase-complete status: GO for Phase 11 (Documentation).

After 10-07-SUMMARY lands, update ROADMAP.md Progress table row for Phase 10 to "Complete" + completion date. STATE.md updates to reflect Phase 11 as the next-to-plan target.
</output>
</content>
